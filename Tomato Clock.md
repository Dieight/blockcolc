# Tomato Clock

> 当前公开版本：`v1.3.1`（Android `versionCode 17`）  
> 当前数据 schema：`7`；地形生成版本：`4`  
> 最后更新：2026-08-13  
> 本文件只保存现行产品契约、开放问题和工作入口。完整旧文档已归档到 `C:\Codex\docs\tomato-clock\history\Tomato-Clock-through-v1.1.2.md`。

## 产品定义

方块钟是一个本地优先、Minecraft 视觉风格的专注计时器。用户每次专注都在持久的 3D 世界中留下建造痕迹；长期投入形成建筑、道路、装饰和自然环境。

优先级依次为：快速开始专注、如实记录投入、形成持久世界、保持任务管理安静紧凑。游戏化不得篡改现实进度，也不得制造羞耻或虚假反馈。

## 当前边界

- Android 是主要发布平台；Web 共享业务、UI 与 Three.js 渲染代码，可作为本地 Web 应用运行。
- iPadOS 无限期搁置；Windows 优先走 PWA，暂无独立客户端排期。
- 单用户、本地离线、无账号、无云同步、无后台分析。
- 用户数据、蓝图和资源包默认只保存在设备上；跨设备通过完整备份文件迁移。
- 不做联机、服务器世界、AI 自动拆任务或在线建筑市场，除非用户明确扩展范围。

## 任务与建筑

### 普通大型任务

- 一个大型任务对应一栋建筑，包含用户手动建立的小任务。
- 每个小任务默认等权，整体进度为各小任务进度的平均值。
- 现实任务进度由用户汇报；建筑按连贯施工顺序映射，避免屋顶、家具等悬空出现。新导入蓝图使用支撑优先顺序；已有已保存蓝图保持其原有顺序，不在用户已有进度中静默重排。
- 首次产生正进度后锁定小任务的新增和删除；仍允许改名和长按排序。
- 可同时保留多个未完成大型任务，但任一时刻只有一个当前任务。
- 完成的大型任务封存为纪念建筑，不再腐败。

### 习惯任务

- 习惯任务没有有限的小任务清单，以重复完成建筑周期表达长期积累。
- 每栋建筑默认需要 `10` 轮，可在设置中统一配置为 `10-30` 轮；周期开始后该值和建筑均冻结。
- 完整专注和提前完成均推进一轮；完成后用户选择下一栋建筑继续。
- 习惯任务和普通任务使用独立专注时长。
- 删除习惯任务不删除已经完成的建筑。

### 进度状态

以下状态必须分离：

- `taskProgress`：用户报告的现实任务进度，永不倒退。
- `buildingCompletion`：任务进度映射出的施工完成度。
- `buildingCondition`：建筑保存状况，可腐败和修复。
- `focusHistory`：完成、提前完成和中断的追加历史。
- `dailyGoal`：按本地日期记录的每日轮次目标。
- `decorations`：每日目标产生的环境奖励。

## 专注与每日目标

- 计时器不暂停，以持久化的 `startedAt` / `endsAt` 和真实时间恢复状态。
- 用户可选择 `1-4` 轮；多轮中每轮结束都先汇报，再进入休息，休息结束由用户启动下一轮。
- 未开始时主时间显示完整计划时长，计算为 `专注时长 x 轮数 + 休息时长 x (轮数 - 1)`；开始后立即切换为当前专注或休息阶段的权威倒计时。
- 单轮专注无论自然完成还是提前完成，汇报后直接结束，不进入休息中间态。
- 中断不推进任务或习惯建筑，但中断前的可靠实际时长计入有效专注分钟。
- 完整和提前完成都计入今日目标；当天启用目标前已完成的轮次要追溯计入。
- 每天按本地日期自动开启默认 `8` 轮目标，调整后立即保存，不需要保存按钮。
- 热力图默认显示近 `26` 周有效专注分钟，分级阈值为 `0 / 90 / 180 / 270 / 360`；月份标签按实际占用周数布局，狭窄的首尾残月可省略标签，但不得相互重叠。
- Android 在专注结束和休息结束安排本地通知；精准闹钟权限不可用时允许尽力提醒，但状态恢复必须正确。

## 腐败与修复

- 腐败默认关闭；开启后默认宽限两个计划专注日，默认修复倍率 `2x`。
- 用户配置每周计划休息日，并保存 IANA 时区；计划休息日不计作中断。
- 腐败只改变 `buildingCondition`，不得让历史完成重新变成未完成。
- 已完成并封存的纪念建筑不再腐败。

## 蓝图、材质与世界

- 内置三套原创建筑；本地 `.litematic` 导入必须先校验、预览，再保存规范化蓝图。
- 主建筑限制：水平占地不超过 `48 x 48`、高度不超过 `128`、非空气方块不超过 `100000`。
- 导入蓝图可选择作为建筑或每日奖励装饰；蓝图库条目可改展示名并持久化。
- 设置提供未建部分的显示范围：关闭、仅当前建筑（默认）或全部建筑。施工轮廓只显示未建结构的外壳，并受昼夜环境光影响，不作为夜间白色自发光体。
- 实体、方块实体和计划刻不重建；未知或不支持的内容使用原创安全回退并诚实提示。
- 用户可导入兼容的 Minecraft Java 资源包；项目不分发 Minecraft 官方纹理或无授权资产。
- 资源包导入保留有界安全预算：ZIP 不超过 `32 MB`、相关单文件不超过 `4 MB`、展开总量不超过 `64 MB`、最多 `8,192` 个文件。Android 大资源包通过缓存令牌和 `256 KiB` 分块桥接，不把整包作为一个 Base64 字符串交给 WebView；超限提示必须指出具体维度。
- 世界使用稳定种子、地形生成版本 `4`、流域水文、汇流增宽河网、湖盆、建筑/道路保护区和近中远 LOD；版本 4 减少零碎水面和远景方形水片，只抬高山地上尾并延长山脊，已有自然山谷按稳定种子重建派生地形。
- 建筑落点必须在整体圆形支撑范围内，不能通过事后破坏地基轮廓来容纳大型建筑。
- 聚落交互采用固定世界、相机环绕；渲染按需唤醒，避免常驻高负载循环。
- 默认构图只按聚落核心计算；相机裁剪范围必须覆盖完整外围地形，最远缩放和任意旋转角度均不得让下缘山体消失并露出虚空。
- 夜间星空锚定相机天空层，并在聚落最远缩放下保留足够的近裁剪安全距离；缩放不得让本应可见的星星整层消失。
- 光影只实现方块钟原创 WebGL2 效果，不执行或分发 Iris/OptiFine shader pack；第三方 shader 仅作为观感研究材料。
- 设置提供“自动 / 流畅 / 均衡 / 精致”四档光影质量。自动档按设备与场景选择，运行中仍可因持续慢帧或场景压力降档。
- 精致档使用半分辨率 Bloom，并增强水面、玻璃与夜间发光；旋转、缩放和原生触控的每个按需渲染帧同步更新光晕，避免屏幕空间光晕残留在旧位置或发生整屏明暗跳变，所有档位保持按需渲染。
- 可见灯具柔光必须锚定真实发光方块并保持方块化的小尺度外观；地形水面必须写入深度并阻挡天空星点，不能以穿透大陆或水面的星点伪装倒影。
- 导入资源包图集在瓦片隔离范围内生成安全 mipmap；精致档离屏场景使用受设备能力约束的轻量多重采样，降低远景纹理与几何边缘摩尔纹，不引入时间累积或常驻渲染循环。

## 数据与恢复

- IndexedDB 是当前生产存储；领域状态与平台文件、通知、生命周期能力通过 ports 隔离。
- schema 迁移必须严格、可测试并保留历史数据；当前 schema 为 `7`。
- 备份导入流程为：预览 -> 验证 -> 创建回滚备份 -> 原子替换；不做记录合并。
- 新建大型任务草稿仅保留在当前进程内；切换页面不丢失，取消、创建成功或进程彻底结束时清除。
- 用户现实完成历史不可被回滚、腐败或视觉状态覆盖。

## 交互与视觉

- 主导航为聚落/计时、任务、统计、设置四个工作区；首屏是可操作产品，不是营销页。
- 3D 世界是情感锚点，当前专注动作是操作锚点；每个状态只有一个主操作。
- 竖屏和横屏计时界面应在一个视口内完整展示，不靠无信息增益的整页滚动。
- 高屏手机可适度放大世界与主时间，短屏使用固定紧凑尺寸；任务、统计和设置页滚动末端只保留导航栏与安全区所需间距。
- 任务页只在存在多个任务时显示跨项目总览；总览位于当前任务详情之后，使用与其他任务分区一致的标题和触控尺寸。统计趋势使用“小时 / 分钟”的完整有效专注时长，不使用“近 7 天分钟”一类把单位当指标名的表达。
- 移动端触控目标至少 `44px`；图标按钮有可访问名称，键盘焦点清晰，不依赖 hover。
- 中文 IME 必须支持组合态、中段插入和候选确认；不得提前提交、重复输入或强制追加末尾。
- 视觉规范以 `tomato-clock/DESIGN.md` 的现行章节为准；与本文件冲突时以本文件为准。

## 工程与验收

- TypeScript 承载共享领域与应用逻辑，React 承载 UI，Three.js 承载体素世界，Capacitor 承载 Android 原生能力。
- 产品改动先写入当前版本工作包，再实现、测试和更新状态；不得把讨论中建议伪装成已确认范围。
- 编辑循环只运行受影响包的类型检查、单元测试和精确命中的定向 E2E；不因每个小改动重复完整浏览器矩阵。
- 检查点门禁使用 `npm run test:fast`，统一执行版本一致性、全工作区类型检查和全部单元测试；在一项完整功能或修复结束、交给人工验证前运行。
- 发布门禁使用按平台职责拆分的低并发 E2E：移动端承担完整产品流程，桌面端承担多视口、鼠标/指针和桌面环境交互，渲染与光影回归跨端执行；随后完成 Web 构建、Capacitor 同步、Android JVM 测试、Release Lint、正式签名 APK 和真机自动化。
- 发布准备中的 Android 构建复用已通过的版本、夹具与 TypeScript 结果，不在嵌套脚本内重复相同检查；发布阶段的暂存状态、上传、重下、安装和哈希比较仍是不可省略的制品边界验证。
- GitHub Actions 第一阶段只处理已提交源码：PR 执行检查点门禁，`main`、`release/**` 和手动运行继续执行浏览器发布门禁并生成未签名的 Release 候选；Actions 固定到完整提交摘要，默认只有只读仓库权限。
- 第一阶段不向 GitHub 上传正式签名材料。CI 候选必须记录来源 commit、包版本、大小与 APK SHA-256，完成来源证明和上传后重下比较；它不可安装或发布，正式签名、真实本地夹具、ADB、真机 GPU/WebView 与人工观感仍由本地发布门禁负责。
- 影响日常流程、渲染性能或主要 UI 的大更新后，先执行 `adb devices -l`。存在已授权设备时，覆盖安装当前正式签名候选包并完成可自动化检查；无设备时明确记录缺口。
- 编译成功不等于视觉验收。3D 画面需检查非空、构图、交互、性能；UI 需检查长中英文、键盘、安全区、遮挡和触控范围。

## 制品哈希约束

- 构建、复制、上传、重新下载、安装和交付的每个制品边界都必须计算 SHA-256。
- 必须把当前边界的摘要与上一边界或受控清单中的期望值比较；只打印一个哈希值不算校验。
- 任一摘要不一致都阻断安装、发布和交付，直到确定差异来源并重新生成证据。
- 真实测试夹具保留在本地，必须按仓库内哈希清单验证；测试源码、配置和合成夹具生成器进入版本控制。
- 发布 APK 还必须校验包名、`versionName`、`versionCode` 和正式签名证书摘要。

## 文档地图

- `C:\Codex\Tomato Clock.md`：现行产品契约、边界、开放问题和工作入口。
- `tomato-clock/docs/versions/V16.md`：当前版本工作包；需求、非目标、风险与验收状态。
- `tomato-clock/docs/versions/V17.md`：当前内部工作包；连续性、建筑记忆、统计、任务组织和资源包兼容验收。
- `tomato-clock/docs/DECISIONS.md`：已确认决策及其影响，按时间追加。
- `tomato-clock/CHANGELOG.md`：面向用户的公开版本变化。
- `tomato-clock/docs/QA-BASELINES.md`：稳定测试基线、设备与已知限制。
- `tomato-clock/TESTING.md`：本地测试、夹具和快速/发布门禁命令。
- `tomato-clock/ARCHITECTURE.md`：模块、状态机、schema 和工程证据。
- `tomato-clock/DESIGN.md`：视觉系统与响应式规范。
- `C:\Codex\docs\tomato-clock\history\`：压缩前的内部历史快照。

## 当前工作

- V13 已完成并发布为 `v1.1.2`。
- V14 工作流、测试可复现性和发布证据改造已完成，未单独增加版本号。
- V15 原创移动端光影增强已完成并发布为 `v1.2.0`。
- V16 已发布为 `v1.3.0`：包含地形生成版本 4、缩小采样抗摩尔纹、计时工作台布局、夜间光源/水面修复与最远缩放地形裁剪修复。
- V17 已完成并发布为 `v1.3.1`：强化工作连续性、建筑记忆、解释型统计、任务组织和 Android 16x 资源包兼容。
- GitHub Android CI 第一阶段已部署：公开 CI 不持有正式签名材料，未签名 Release 候选已通过来源证明、上传后重下哈希与本地再验证；正式签名和真机验收继续留在本地。
- 正式版本统一使用既有正式签名；debug 签名不用于可分发版本。

## 开放问题

以下问题尚未决定，影响实现时必须向用户确认：

1. 是否内置具有明确再分发授权的默认 16x 资源包，以及备份是否嵌入用户导入的第三方资源文件。

## 维护规则

- 用户确认新的产品决策时，在同一任务中更新本文件和当前版本工作包；仅讨论时不改为已确认状态。
- 已发布版本的细节进入 `CHANGELOG.md` 和 QA/决策记录，不再持续堆入本文件。
- 版本号仅在准备形成累计更新或 Release 时变更；内部推进不频繁递增小版本。
- 当前事实与历史证据分离；不得删除旧证据，归档、复制或上传后都按哈希约束验证完整性。
