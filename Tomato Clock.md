# 方块钟 / Blockcolc 当前产品事实

> 当前代码版本：`v2.0.0` / versionCode `41`（2026-09-25；收尾功能已由用户确认，进入正式全量门禁与发布流程）
> 当前发布版本：`v1.11.0`（实际提交以同名 Git tag 与发布证据为准）
> 最后更新：2026-09-25

本文件只记录当前有效的产品边界、已确认决定和未解决事项。版本实施过程、一次性诊断和历史验收数据不追加到这里。

## 产品目标

- 用专注计时帮助用户持续推进真实任务，并把投入转化为可见的方块建筑世界。
- 一个普通大型任务对应一栋建筑；小任务进度共同决定建筑的永久建造进度。
- 习惯任务通过重复完成专注轮次，持续生成独立建筑。
- 第一操作焦点始终是开始、继续或汇报当前专注；3D 世界是情感焦点。

## 不可破坏的产品边界

- 本地优先、离线可用、单用户，不要求账号或云服务。
- 私有同步采用共用核心源码的标准版与私有同步版，取消独立伴侣 APK；私有构筑直接集成好友服务器上传，标准构筑不包含私人配置或上传实现。同步故障不得影响本地计时与保存。普通无小任务场景采用通用“专注中”，不扩展上传习惯标题；默认补传从接入日开始，实时三项时间均描述当前段。实施与部署状态以同步工作包顶部为准。
- 私有同步允许仅日总量的离线补传；网页优先新状态 SSE 推送，实际不支持时前台每五分钟查询，期间本地估算并标注手机状态距今多久。保活不代表手机同步；不把估算记入实际日总量。仅补接入日及以后，恢复备份通过显式来源切换进行权威修正。
- 私有同步仅导出当前名称（含马拉松/极简/休息）、开始、预计已持续、预计结束、今日实际专注总时间；服务器历史仅保留 UTC+8 日总量与必要技术元数据，不改变本地历史。独立网页不遵循本体视觉规范。私人凭据与部署材料不进入公开源码；不将好友系统或双向云同步纳入范围。见 `docs/handoffs/BLOCKCOLC-RELAY-PLAN.md`。
- 计时真相来自持久化的绝对时间戳；切后台、锁屏或进程恢复不能依赖 UI 递减值。
- 历史任务进度不会因建筑腐败而倒退；建筑状况与任务完成度彼此独立。
- 导入内容在本地校验和预览；不分发未授权的 Minecraft 资源。
- 中文输入法 composition、中间位置编辑、安全区、键盘和至少 44px 触控目标属于发布要求。
- Android 生命周期事实由原生层提供，业务真相仍留在共享领域层。

## 当前产品能力

- 普通任务、习惯任务、多项目切换、小任务排序与进度汇报。
- 普通任务与习惯任务都支持固定轮次和按结束时间安排；习惯轮次直接推进习惯建筑，不进入普通任务的统一进度汇报。
- 每日目标、专注热力图、统计、建筑腐败与修复。
- IndexedDB 本地持久化、JSON 备份/恢复和软删除回滚。
- Litematic 与 Java 资源包本地导入、兼容性校验和原创材质安全回退。
- Three.js 方块世界、自然山谷、经典空岛和海洋小岛环境。
- Capacitor Android 容器以及文件选择、通知、生命周期、输入法和系统设置原生适配。

## 当前工程决定

- 2026-09-25 用户对 2.0.0 (39) 的反馈：极简空闲信息面板必须能从今日页右滑返回时钟页；现实天气继续使用已授权的“天气预报”类别中的实时天气接口，控制台未开放预警、指数、空气质量。定位成功后只在本机缓存上次有效坐标，后续定位暂不可用时可据此再次请求天气；关闭现实天气仍使用本地天气，设置说明须如实告知本机缓存。原始三张内置蓝图补齐合适窗位的玻璃并适度添加内饰，不改用户已保存的导入蓝图。2.0.0 (38)/(39) 的 APK 签名证书 SHA-1 已核对相同；构建号增加不改变同一证书指纹。
- 2026-09-25 收尾决定：世界面板天气提示沿用原悬浮窗的样式与位置，冷启动只展示一次，5 秒后按有效离开次数提示的 180ms 淡出节奏消失；现实天气可用时显示“当前天气：xxx（和风天气）”，否则显示本地天气。`D:\Litematic` 的补充蓝图标题与描述署名以最新文件名为准，稳定 ID 与建筑/奖励分类不变。标准公开构筑不含私人同步上传模块与配置，私有构筑只在本机生成。
- 2026-09-25 极简统计收尾：浅色高透明玻璃下，统计区的小字号标签、刻度、图例和注释使用统计专用的更高对比度文字变量；当前两页轮播保持时钟页与今日时间轴的双向切换，不增加第三页。统计图采用节奏面积 A 方块阶梯，按小时堆叠实际专注的 15 分钟方格；小时内先后不编码，无专注的小时留白。
- 2026-09-25 26.3 材质与加载收尾：远景地形的资源包纹理先从图集页裁成独立小纹理再平铺，避免 LOD 粗格跨采样图集邻色；计时页重新加载材质期间复用全屏加载动画，完成后再恢复世界；加载页移除底部提示。极简统计任务颜色改为克制的低饱和绿灰阶，不再按任务轮换鲜艳色相。

- 2026-09-23 继续 F04：以已正式发布的 Minecraft Java 26.3 为资源包与方块状态兼容基线，不再以预览快照或 26.2 替代；按官方版本数据逐项盘点 ID、状态、模型、纹理、染色与特殊渲染，不把安全回退算作完整适配。上一份 2.0.0/37 APK 由用户深度体验，暂不重建或正式发布。极简空闲信息在原高面板内直接左右滑动切换，移除底部按钮/页码；统计页纪念建筑区像成就区一样可整体折叠。天气尝试复用现有本地/现实天气状态，在极简面板加少量玻璃雨滴与雷电反馈；必须避免遮挡计时、手势和阅读，遵守减少动态并保持低功耗。

- 2026-09-23 蓝图补充：将本机 `D:\Litematic` 的八份 .litematic 纳入下一测试 APK。`WQH的小黄鸭`、`小水箱`进入每日奖励装饰池，其余六份作为普通任务建筑蓝图；两类不能混入对方选择列表。原始蓝图与其内容资产暂留本机排除目录，不随公开源码上传；保留原名、稳定内置 ID、真实预览和旧用户奖励兼容。本机补充建筑创建任务或习惯下一座建筑时须把蓝图快照保存在项目/备份里，以免以后换成不含这些素材的构筑造成历史建筑丢失；正式发布前另核对分发许可。

- 现实天气：2026-09-23 用户改为允许将已有 API Host 与 API KEY 直接集成到 Android 测试构筑，并将在版本完成后把实际包名和签名证书 SHA-1 指纹填入和风天气控制台设置 Android 应用限制。凭据仍只保留在本机被 Git 排除的配置，不进入公开源码、Web 构筑或领域备份；APK 客户端内的凭据可被提取，Android 应用限制降低滥用范围但不等同加密证明。现实天气默认关闭，只有显式开启后请求位置；失败时回退本地天气。完成测试 APK 后从同一签名产物提取并交付包名与 SHA-1 指纹，控制台配置后再验证实际天气请求。

- 2026-09-21：恢复 `docs/TODO.md` 除 F04 方块/材质兼容以外的事项，追加已选小任务→极简结算串线、普通/马拉松活动沉浸入口及马拉松沉浸标题复用修复。新增默认关闭的自动连续专注；用户确认锁屏/后台也需在休息结束后自动开始剩余轮次。必须按持久化绝对时间恢复，不通过仅前台定时或虚构执行成功绕过原生约束。现实天气 Host 后来已补齐；凭据构筑规则以后续 2026-09-23 决定为准。此决定覆盖此前全部暂停，其余发布授权规则不变。

- 2026-09-20 返修：测试交付构建号必须递增，本次 35→36，保留包名与签名以支持数据保留升级。扩大旧 10 MiB 备份导入上限至 100 MiB，保留严格校验与原子回滚；补充独立的一次性网页查看邀请，原七天浏览器会话规则不变。测试 APK 仍仅上传现有私有测试仓库，其余待办暂停。

- 2026-09-20：先收尾当前同应用私有同步与好友服务器，单独交付正式签名的私有测试 APK；其余待办保持暂停。沿用测试仓库仅上传 APK、回读比对哈希和用户人工验收规则，不属于 2.0.0 正式发布。
- 当前交付：修复私有上传调度权限与失败回报后，因手机未连接，直接上传现有私有测试仓库，仅交付 APK；手机到网页的实际更新由用户安装后验收。

- 2026-09-19：此前全部未完改进移入 `docs/TODO.md` 并暂停，包括本轮 R01–R10 真机反馈、右侧今日时间轴参考图与 F04 材质兼容。先做好友服务器同步，两个构筑共用代码，私有版内置上传模块，替代旧独立伴侣方案及旧执行顺序。沿用已确认最小上传范围与服务器部署授权；不扩大为公开发布授权。

- 2026-09-12：配置与 skill 准备已完成，用户确认先完成简单 UI 并交付一版测试 APK，再推进复杂功能。首批为 F01 活动文字、F03/F12 设置输入与说明、F09 今日目标弹层、F10 分配图表；不包含休息状态重构、原生性能、资源兼容或成就契约。主代理 Astra 负责规划、契约、集成和审计，Luna 负责边界明确的执行；覆盖旧“不委派”决定。保留其他代理最新工作树和历史 APK，使用增量 verification 流程，不新增版本或正式发布授权。
- 2.0.0 后续 18 项按现有计时/原生生命周期与通知/资源兼容/世界渲染/UI与成就所有权分组，替换旧分支而非追加覆盖层。范围、依赖与待冻结决定见 `docs/versions/V27-2.0-FOLLOWUP.md`；准备完成不代表功能完成。
- 新增 F19/F20：马拉松/极简汇报需支持给有进度变动的普通任务分配轮次，并修正习惯轮次的统计归属；统计页新增纪念建筑板块，展示已完成大型任务及小任务投入分布，沿用图表 skill 与应用配色。归属、实际时长换算和旧记录可追溯性先冻结契约，不能把没有依据的历史时间平均分摊。两项属于后续数据/统计切片，不扩大首批 UI 验证包。
- 2026-09-13 用户反馈：设置数字输入的单位移到左侧字段说明末尾，数字框贴齐行内容右边缘，不保留右侧单位占位列。全量测试与子代理等待仅由完成事件触发检查，不再每 10 分钟检查；测试 APK 安装后由用户人工验收，不再进行代理手机 UI 巡检。仍保留构建、包身份、签名和交付/安装哈希核对，不清数据。
- 2026-09-13 最新出包范围覆盖首批 UI 优先方案：设置单位返工不单独出包；继续完成 F04 材质兼容之外的全部后续项，再集中测试并构建新测试 APK。仍不授权正式发布/版本提升。待继续显示保留剩余专注与后续休息合计；历史新成就在升级后首次进入统计集中展示一次、可全部关闭，之后仅提示新解锁。

- 2026-09-09 用户恢复 UI 工作：本轮由当前代理直接设计、实现和验证，不再交接。计时面板统一、极简手势重设计、四时段图标、统计精简和任务页打磨纳入当前 V27；覆盖旧 UI agent 所有权和旧接续顺序。保留核心重构与本地数据，不增加版本或发布授权。

- 2026-09-08 用户调整优先级：暂缓继续 UI 返修与伴侣部署，直接推进核心编排重构。保留最新 UI 工作树，不更换技术栈、领域/备份格式或既有计时含义；核心本轮持有 App.tsx，按轮次上下文、命令结果、视图/流程与渲染宿主逐片接线验证。当前检查点见 `docs/handoffs/V27-CORE-RESULTS.md`，此决定覆盖下条旧接续顺序，不增加发布授权。

- 2026-09-07 接续顺序已确认：先独立审核UI返修，完整记录V27及伴侣部署剩余任务，再继续私人伴侣/部署。最新执行入口为 `docs/handoffs/V27-REMAINING-HANDOFF.md`；原生出口与Kotlin同步现有代码仍待安全/真机验证，不能视为可发布。服务器部署和本机WSL Docker安装已获授权，但没有公开伴侣源码/APK或主应用正式发布授权。

- 继续使用 TypeScript、React、Three.js、IndexedDB 和 Capacitor，不启动 Android 全原生迁移。
- Android 是主要发布平台；Web 实现继续作为共享 UI、浏览器运行和自动化验证边界。
- 日常验证按改动爆炸半径选择；完整稳定 E2E 只用于发布、夜间或跨领域高风险改动。
- 临时 probe、diag、measure 和 dump 不属于默认回归套件。

## 当前稳定体验规则

- V25 以稳定性和日常闭环为主题，不加入新的世界环境、云同步或 Android 专项适配。
- 修复并验证 V24 遗留回归；没有可复现证据的问题不以猜测方式改动产品行为。
- 发布前收敛海洋小岛的天空可见空洞/接缝：顶面补丁与 2/4/32 单位混合 LOD 侧面都按实际发出网格闭合，并以 9 栋建筑的大聚落样本覆盖远坐标带。
- 海洋主岛使用种子驱动的非对称岸线、偏置山脊、缓坡台地、岩质岬角与海湾沙滩；只复用自然山谷的建筑/道路支撑融合机制，不复用山谷视觉，也不保留圆形或方形建筑平台。
- 海洋环境的建筑地基以主岛可居住台地为最低基准，不再随机下沉；主岸线始终使用近/中景格网，岩石形成外伸岬角、沙滩落在内凹海湾，避免矩形突出和机械拼接。
- 发布前统一普通大型任务及小任务的中文输入法提交边界；中文、英文、空格混合输入和名称编辑必须原样持久化。
- 收敛 Web CSS：停止版本式覆盖追加，建立语义变量和功能所有权，并移除 `styles-overrides.css` 运行时入口。
- 拆分 `apps/web/src/App.tsx` 的主要功能组件，保持领域、存储、计时和原生适配边界不变。
- 落成“建筑记忆”：在聚落中点选建筑后，以世界容器之外的视口级模态层展示项目、施工、保存状况和既有专注记忆；竖屏面板贴紧底部导航上沿，横屏/桌面贴紧标题栏下沿与屏幕右侧。建筑聚焦与记忆面板分别持有状态：关闭面板或继续其他任务保留当前建筑和相机；“重置地图”清除建筑聚焦并返回完整聚落，“重置视角”只重算当前构图。
- 深色模式下当前小任务/习惯建筑的左侧状态条使用深绿色按钮同色；专注保护中断只显示一个不遮挡主按钮的局部提示。
- 设置提供本地持久化的“显示世界坐标”开关；旧版已有偏好迁移时保持开启以便候选验收，用户关闭后无需重建 APK。
- V25 为多轮专注休息接入 Android 系统持续倒计时：Android 16 请求 promoted ongoing Live Update，系统按绝对结束时间绘制 chronometer；较旧系统或系统拒绝 promotion 时降级为普通持续通知。展开卡显示轮次、下一项任务和“跳过休息”；设置页展示系统真实允许状态并可进入 Android 官方实时通知设置。OPPO 流体云直连仍是需要平台准入的增强路径；系统决定胶囊布局，产品不能承诺把分钟和秒分别固定在摄像头两侧。
- OnePlus 真机已确认系统实时通知会同时呈现为流体云和通知抽屉卡片，两者是同一条通知而非两次发布。实时状态与到点提醒使用不同通知 ID，重复生命周期刷新不得取消并重发当前流体云。
- 沉浸计时保留原有直边贴屏操作带，只把材料替换为与世界内容分离的自适应毛玻璃；设置提供本机持久化的通透度滑杆。`0%` 是高色调、高扩散的 Frosted/Regular 端点，`100%` 是低色调、低扩散并保留可读性压暗的 Clear 端点；中间使用非线性完整材质映射。面板边缘不使用硬描边或 inset 双高光，只以向世界侧扩散的软阴影建立层级。世界 HUD、坐标/重置视角、建筑记忆和瞬时状态提示复用固定高可读性玻璃；其他表面按现行场景材料规则选择，不能因材料改变已验收的几何布局。偏好不进入领域 schema 或备份。
- V27 设置页整体迁移到 Apple 设置样式的液态玻璃分组列表：分组标题在卡外、行收纳进圆角玻璃卡并以发丝线分隔；文字选项组改为分段控件，开关改为 iOS 药丸样式，"显示世界坐标"迁入新增的"高级"分组。字体、语义配色与 44px 触控契约不变；材质 token 浅/深双主题入库，`prefers-reduced-transparency` 回退实底。该设计取代 V18 的"去盒化"平面行样式。
- Android 硬件返回键逐层退出：结束专注对话框、计划单、沉浸结束按钮、建筑记忆、关于、落成仪式与创建草稿各自作为一层，自顶向下派发；栈空时非计时页签先回计时页，计时页空栈才退出应用。
- 需要用户决策的状态提示（通知降级/不可用）在 toast 上附带"去设置"动作并可点击跳转；确认型提示保持纯文本指针穿透。有效离开警示仍走世界页五秒警示片，不占用全局 toast。
- 活动专注面板不显示建筑施工阶段或完成百分比；所有专注模式共用面板材料、几何和计时组件，只按状态替换必要文字及操作。
- 启动页方块为依次落定的铺设动画；深浅主题切换对壳层颜色做短过渡；两者均遵守 `prefers-reduced-motion`。

## 已确认的 V26 范围

- V26 以“快速、可恢复、可发布”为主题：用户收益是更快启动、进入世界和恢复专注，工程收益是更短但覆盖不缩水的反馈与发布链路。
- 冷启动时并行请求计时、任务、统计和设置四个主要页面，并让非当前页面保持挂载和隐藏；启动只呈现一次统一加载过程，之后第一次和后续切页都不得再创建页面、计算首次投影或执行挂载副作用。分包继续用于控制入口体积，不再把解析成本延后到用户第一次点击。
- 建立启动、首个非空世界帧、世界重建、交互帧、内存和 Android 恢复基线；只对影响世界内容的输入重建 Three.js 世界，展示层变化不得触发地形重建。
- 按实际职责继续拆分应用编排、渲染输入/帧调度和地形生成边界，让陈旧指针、环境调度、LOD 邻接与网格发射可以通过确定性单元测试验证；不以机械降低文件行数作为完成标准。
- 完整 Web 发布门禁拆为可并行的核心业务、必须单 worker 的 3D/资源包与响应式视觉套件，最终仍合并为同一冻结源码的一份完整结果；不拼接不同源码树或失败运行的局部结果。
- 正式候选在完整门禁后只构建一次。用户最终验收、GitHub 上传、重下载和设备安装必须对应同一源码指纹与 APK SHA-256；正式发布阶段不再重新构建。设备忙只暂停可恢复的真机安装阶段，不使已通过且源码未变的完整门禁失效。
- 收口 Android 锁屏、后台、最近任务上滑、进程回收、休息跳过和 Live Update 幂等更新矩阵。系统强制停止后的通知限制必须如实降级，重新打开时仍按持久化绝对时间恢复业务真相。
- “今日下一步”在 V26 真机验证后撤回：它与按结束时间的马拉松计划目标重叠，却增加排序步骤并限制为三项，不能形成足够独立的用户价值。UI、命令和领域字段全部移除；短期测试版写入 schema 10 的该字段仅在读取时定点丢弃，其余用户数据保持不变。
- 马拉松统一汇报中的习惯任务一律从 `0` 轮开始；展开卡片只查看详情，不得自动占用可分配轮次，只有用户点击增加按钮才形成分配。
- 海洋环境的建筑、道路、道路支撑融合和路灯共用最低 4 格的可居住台地基准，避免建筑抬高后道路与灯具仍按旧山谷高度向下开槽。
- 马拉松无论由普通任务还是习惯任务发起，都统一使用“普通任务专注”分钟计算排程、每轮倒计时和剩余总时长；习惯任务自己的专注分钟只用于固定轮次。取消或结束马拉松后立即回到当前任务的默认一轮时长，不保留旧结束时间。
- Android 活动专注与休息共用一条互斥的系统实时倒计时表面：专注折叠态统一使用短文案“专注中”，马拉松语义只在展开卡显示，避免占满摄像头旁的状态区域；通知明确使用公开可见的计时器类别并提供绝对 `endsAt`、countdown chronometer，让支持标准 Live Update 的锁屏/息屏层可以读取剩余时间。小图标使用纯白镂空番茄，并显式要求 OxygenOS 不以应用图标替代；WebView 不在后台逐秒运行。同一计时器的生命周期恢复复用现有通知，新计时器使用新的系统记录身份，避免息屏卡片继承旧文字、旧图标或旧结束时间。PJX110 已确认通知抽屉倒计时正确、息屏能读取最新图标与专注/休息状态，但当前 OxygenOS 息屏模板只取两行状态而忽略标准 chronometer；这是如实保留的 OEM 降级，不以后台逐秒重发通知换取高耗电和闪烁。
- Android 专注保护只消费原生活动层合并后的单一注意力方向；`pause/resume`、窗口焦点、多窗口和 OEM 小窗不得作为彼此独立的计数来源。离开时间只在真正转为后台时记录，回到前台不能覆盖它；通知栏停留和应用后台往返都只形成一次离开/返回对。领域结算新增有效离开后，App 根层必须先保存生命周期回执，再把这条一次性信号交给常驻世界页驱动既有的五秒悬浮警示片“有效离开 X / Y 次”；不能依赖子组件在同一窗口事件传播期间保持挂载，也不能另起一条全局 Toast。
- V26 不增加新环境、Shader、云同步、Android 全原生迁移、ColorOS 私有接口或新的资源包兼容范围。

## 已确认的 V27 范围

### 2026-09-12 后续决定（覆盖下方冲突的旧方案，实施状态见当前工作包）

- 极简与马拉松活动文字统一为“专注中 第x/n轮”。所有模式的休息/待继续统一沉浸表面：倒计时配“休息中”，休息结束后剩余轮次总时间配“剩余专注总时间”，复用活动文字槽；休息不再提供返回完整模式按钮。移除非沉浸专注/休息旧页面及冗余编排，不能因此删除必要的中断、汇报、计划恢复或失败补偿。
- 极简/马拉松还有剩余轮次时，休息结束触发默认开启、可关闭的返回专注提醒；使用系统实时表面及通知栏。具体 OEM 展示需真机验证。优化双击开始下一轮和通知栏收起后世界旋转的延迟，先测事件/存储/渲染路径，不以增加延时或重建遮掩卡顿。
- 设置数字输入、完整性说明与开关对齐、今日目标内部弹层、统计分配图表和图标继续收口；废弃图标外围白色刻度方案，保留应用语义配色及可调/固定玻璃边界。
- 首次启动必须且仅创建一个普通大型任务或习惯任务。成就在进入统计时检查，新增解锁以包含内容和获得时间的动画弹层展示，并显示已解锁/未解锁项目；扩充目录。一次性展示、历史回溯及无法推导获得时间的处理待契约冻结，不虚构时间或每次进入重复庆祝。
- 扩大本地 Java 方块/材质视觉兼容，目标为实施时固定的 MC 26.3 最新测试版本，官方资料不足时回退 26.2 正式版；颜色、纹理和造型均纳入逐类验收，不能把导入成功或安全回退声称为完整兼容。不扩展为完整 Minecraft 游戏模拟或分发未授权原版资产。
- 用户要求确保 60×60×60 蓝图可用；当前解析器已有横向96/高度256限制，先追查实际入口瓶颈，不直接收紧现有限制。日/月方向需随时间变化；新增符合环境的通用/专属装饰（如花、海洋少量露出水面的沉船）、修复小预览云堆积并审查各环境地基与地面关系。保持历史进度、既有布局身份与已修海洋地基不倒退。

### 先前确认与验收背景

- 2026-09-09 真机返修：极简空闲选时滑回当前时刻时清除草稿并恢复实时钟；中断本轮保留已确认的马拉松计划，待继续显示剩余轮次与间隔休息合计时长，不能伪装成 00:00 或新建计划。退出完整界面后，未完成的极简计划始终保留重新进入极简的入口。
- 统计沿用应用原有绿色与浅深主题，不再套用模板灰阶外观。固定 26 周日历在时间范围统计之外独立显示，直接点击日期查看记录，键盘提供等价查询；不再以底部日期输入框作为主要入口。
- 任务页的今日目标（全局进度）、任务选择、当前任务详情、任务总览和任务管理统一背景卡、标题与正文层级；触控不出现浏览器蓝色高亮，保留清晰的键盘焦点。
- 玻璃消费者按作用统一：只有沉浸计时带（普通专注及极简空闲/专注/休息/待继续）跟随通透度设置；计划调整与任务选择菜单共用固定阅读玻璃，设置分组保持固定轻玻璃，世界 HUD/提示/建筑记忆保持固定功能玻璃。正文、统计、输入及破坏性确认保持实底。固定材料随浅深主题切换，不随通透度滑杆变化；集中 token 定义，减少透明度统一回退。

- 2026-09-09 新设计覆盖下方旧按钮规则：极简空闲常态只显示当前时间，上下滑动时间选择结束点、双击所选时间直接开始；休息常态只显示真实剩余时间，双击倒计时跳过休息并开始下一轮。双击面板空白处显示/隐藏返回完整模式，不能同时触发计时操作；键盘提供等价操作，草稿与持久化计时分离。正常计时页的极简和任务入口相邻。
- 应用图标改为四时段不规则色块表盘：上午 06–12、下午 12–18、晚上 18–24、午夜 00–06；番茄边框简化成线条，无外围白色刻度。2026-09-13 用户已批准四时段色块预览方向，母版为 `apps/android/icon-source/blockcolc-four-periods-approved-20260913.png`，使用现有脚本派生启动图标。保留已验收的原生镂空通知图标。
- 统计页将常看指标前置，去掉重复与低价值常驻统计；成就放在底部并可折叠。图表参考 lieflat-charts 真实模板，保持本地离线、数据编码诚实。任务页统一层级和细节，液态玻璃仅用于有实际层级收益的位置。

- 2026-09-07 最后按钮方案确认：极简空闲“开始专注”采用半透明森林绿玻璃；浅色淡绿底/深绿字，深色深绿底/浅白绿字。保留现有圆角矩形、布局及触控尺寸，仅轻微边缘高光和背景模糊，不加发光、呼吸动画或额外图标。通透度跟随液态玻璃滑杆，但文字及底色保留可读性下限；减少透明度时回退可读绿色实底。当前仅方案已确认、交接下一位实施，见docs/handoffs/V27-MINIMAL-START-BUTTON.md。

- 2026-09-06 真机反馈：计划专注日改输入框风格圆角矩形，七项单行等宽；完整性开关与上限分行，仅开启时显示上限及可配置离开秒数。离开阈值默认 3 秒，本轮实现范围 1–60 整数秒，严格超过才计数，随领域备份保存并迁移旧数据。极简空闲/运行时间共用居中几何，主题色玻璃开始按钮，返回完整模式复用双击显示，专注不显示建筑进度；结束时间与马拉松共用输入展示。进入极简的小按钮移至任务标题旁；冷启动只回滚动画为旧版方块起伏，不撤回预加载逻辑。详见本轮真机反馈交接。

- 用户授权本次极简模式与成就系统集成后，由 UI agent 完整验证并构建版本名 `2.0.0`、正式签名的测试 APK 供用户验收；它不是线上发布授权，也不将 V27 其他遗留项自动算作完成。versionCode 保持单调递增，本轮计划为 27。

- V27 延续 V26，主题为统一体验与工程边界：收口已知回归，按状态所有权拆分应用编排，优化实际更新成本，统一 UI、视觉资产流程和现行文档。保持当前技术栈、本地优先与既有业务含义。
- 液态玻璃是明确保留的设计方向：世界悬浮层、建筑记忆和合适的菜单／Sheet 优先使用；设置分组采用轻玻璃，减轻白色覆盖与过重投影。普通内容、图表、输入及关键决策区以可读性为先，不为了玻璃新增装饰背景或持续渲染。导航本阶段保持实底；沉浸计时的直边、大小和通透度映射保持已验收结果。
- UI agent 负责展示组件、页面一致性、CSS 所有权、视觉资产规范和受影响验证；核心 agent 负责 App 编排、领域／存储／原生／渲染边界和性能，集成负责人审计与收口。独立 worktree 隔离工作，公共文件明确单一负责人，不把新对话视为目录隔离。
- V26 真机仍存在“离开计数正确、既有五秒提示不弹”的未解决问题，列为 V27 核心修复；旧工作包的自动通过记录不代表此问题已通过真机验收。OnePlus 息屏不读取标准 chronometer 继续记录为 OEM 降级，不用后台逐秒重发通知弥补。
- UI 统一覆盖浅／深色、44×44px 真实触控区域、IME、键盘／安全区、减少动态与透明度；共享组件提取和 CSS 清理按实际职责推进，不以拆文件数量或删除行数作为收益。
- 资产区分批准母版、派生产物、来源／许可及校验；沿用现有图标生成基础，保留已验收的镂空通知番茄、蓝图稳定标识、施工顺序与用户导入事实。
- 2026-09-14 测试交付：非材质改进通过统一验证并构建后，手机未连接时仅上传测试 APK 到现有私有仓库 `Dieight/blockcolc-test`，保留旧包，不推源码或其他文件；上传后回读比较 SHA-256。手机连接则覆盖安装，不做代理安装后 UI 巡检，用户人工验收。此授权不包含正式发布。
- UI agent 在独立工作树返修，核心 agent 已获授权并行推进非 UI 重构；每轮审计后必须回写 UI 交接文档，包含问题编号、真实证据和下一轮验收条件。仍沿用验证 APK 增量测试、正式候选冻结树全量门禁的两阶段流程，不自动合并、安装或发布未验收的 UI。
- V27 增加轻量成就系统：本地成就墙与安静轻提示，从已保存的专注、任务及建筑事实回溯解锁；不绑定奖励、账号、云同步或新的每日打卡负担。由核心提供确定性投影，UI 只展示；历史回溯不连发解锁提示，不凭成就改变已有进度。
- V27 极简模式：本机开启后启动直接进入复用的沉浸页面，空闲显示当前时间、不显示小任务名；专注与休息复用沉浸材料和几何。入口只选马拉松结束时间，不选择任务和轮次，保留返回完整界面的路径。用户确认不绑定小任务、全部完成后复用马拉松统一提交进度，习惯分配从零开始，不逐轮自动推进。底层可保留既有宿主引用但不创建隐形任务；schema 11 显式保存延后结算标识，旧记录迁移保持原语义，旧版应用不保证读取新备份。活动计时、待汇报和恢复错误优先于空闲时钟；无项目首次设置规则暂不扩展。
- V27 调研并适配 iQOO / OriginOS 的原子通知与原子岛。继续使用绝对结束时间和单一互斥通知；标准 Android 倒计时为回退。厂商场景准入、机型/系统及实际倒计时能力必须核实；不伪装其他场景、不擅自申请准入或引入云推送，不承诺未经真机验证的息屏表现。

## 文档地图

- 当前产品事实与确认决定：本文件。
- 当前工程边界：`ARCHITECTURE.md`。
- 当前视觉与交互规则：`DESIGN.md`。
- 测试分层和命令：`docs/TESTING.md`。
- 当前及后续版本工作包：`docs/versions/V*.md`。
- V27 UI 分工、材料场景与交回验收：`docs/handoffs/V27-UI-AGENT.md`。
- V1-V24 的历史实施流水：`PROJECT.md`，已冻结，不再追加。
- 研究与兼容性背景：`V2-RESEARCH.md`、`MINECRAFT-VISUAL-COMPATIBILITY.md`。

## 更新规则

- 用户确认的产品决定在同一次变更中更新本文件和当前版本工作包。
- 新版本新建一个 `docs/versions/VNN.md`，不在顶层文档末尾增加新的版本章节。
- `DESIGN.md` 和 `ARCHITECTURE.md` 直接修改现行规则；被取代的规则由 Git 历史和版本工作包保存。
- 测试数量、构建哈希和一次性真机数据只记录在对应版本工作包或机器生成的发布证据中。
