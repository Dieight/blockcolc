# 方块钟待办

更新：2026-09-25。2.0.0/41 收尾构筑正在准备，标准候选不含私有同步上传，私有集成 APK 只留本地；当前 F04 继续以 Minecraft Java 26.3 正式版为目标。下列真机反馈并入本轮，正式发布仍需对应候选人工验收。

## 已交付，待人工验收：双构筑与好友服务器同步

- [x] 标准版与私有同步版共用核心源码，构建时隔离上传能力；不再开发独立伴侣 APK。
- [x] 接通提交后的五字段快照、准确休息起止、UTC+8 日总量及断网补传（代码与自动化验证完成，手机验收见下）。
- [x] 私有版本原生上传、鉴权、退避、合并与后台恢复；失败不影响本地专注。补齐来源切换幂等与活动上传取消后 pending 恢复。
- [x] 完成私有网页实时看板、日历、SSE 与前台五分钟查询回退；汇总热力图每 2 小时一档，仅改网页不改 App。
- [x] 服务协议/鉴权、容器限制、SQLite 恢复与部署；公网静态文件哈希、未登录拒绝与 SSE 实际代理链路验证通过。
- [x] 私有 APK 签名及覆盖安装；标准 APK 确认不含私有域名、上传凭据或 PrivateRelay 实现。私有包已安装 PJX110，设备哈希一致，不清数据。
- [ ] 用户人工验收：现有数据与计时连续性、手机到网页的专注/休息切换、断网补传、锁屏/OEM 后台延迟。未做代理安装后 UI 巡检。

## 当前执行：本轮真机反馈

- [x] R23 三张原始内置蓝图的空窗补玻璃并适度补内饰；只改变内置模板，不重写既有导入蓝图或项目事实。定点与 Web renderer 回归通过，待真机画面验收。
- [x] R15 将本机 `D:\Litematic` 十份蓝图打入测试 APK：小黄鸭、小水箱、神秘附魔台作为每日奖励，其余七份作为普通建筑。保留最新文件名、稳定 ID，走现有预览/世界/奖励校验和旧用户幂等初始化；原始内容暂不进入公开源码。2.0.0/41 构筑前已重新校验并打包成功（约 2.18 MB 蓝图 JSON），含告示牌/营火数据；待用户真机验收。

- [x] R11 已选小任务时进入极简并开始，无绑定并延后统一结算；Web release 回归通过。
- [x] R12 普通/马拉松/极简共用活动与休息沉浸页面，启动路径不以 UI 模式改变业务模式；Web release 回归通过。
- [x] R13 马拉松沉浸页采用共用计时与轮次文字槽，移除继承的任务详情；Web release 回归通过。
- [x] R14 自动连续专注设置默认关闭，按绝对时间支持休息后继续、进程恢复、幂等和写入失败边界；单元与 Web release 回归通过，锁屏/OEM 行为仍待真机验收。

- [ ] R01 专注拉下/收回通知栏卡顿已缩短但仍有 1–2 秒；本轮合并连续安全区 WebView 更新并跳过被系统遮挡时的纯后台界面刷新，完整性后台/前台持久化均保留。10.25 MiB 合成状态测得一次拉下/收回仍有两次必要写入，耗时波动大；并非真机证据，仍需设备上区分存储、系统安全区和 GPU/画布耗时，不靠延迟掩盖离开判定。
- [ ] R02 纪念建筑默认全部折叠，修复点击后不可退出的白屏。
- [ ] R03 查明各聚落装饰不可见，验证实际地表位置、密度和镜头可见性。
- [ ] R04 蓝图库导入复用新建任务的建筑预览。组件接线与真实导入定点 E2E 已通过，待真机视觉验收。
- [ ] R05 汇报普通任务选择“保持进度”后也允许明确分配轮次。
- [ ] R06 精简普通/习惯轮次分配控件，统一视觉与触控尺寸。
- [ ] R07 不可追溯提示精简并合入纪念建筑顶部说明。
- [ ] R08 统计成就区与其他统计板块统一 UI。
- [ ] R09 极简未专注时横滑面板内容，外部面板固定；右页显示今日总时长、轮次/目标。按用户参考图采用一天 00–24 时坐标轴、按小任务分色的时间分布；保持实际时间与未归属数据诚实，不用平滑曲线虚构专注区间。代码与移动端定点 E2E 已完成，待真机视觉验收。
- [x] R10 统一各环境天气视觉，保留环境概率差异；现实天气默认关闭，开启后请求位置并通过和风天气同步，处理鉴权、定位拒绝、断网与本地回退。Android Host/API KEY 从本机被排除配置进入构筑，REST 请求附包名及实际签名证书 SHA-1；Web 不含凭据。仅本机缓存上次有效坐标，在仍获定位授权但本次定位失败时请求天气；设置如实说明缓存。定点与 Android 构建门禁通过，真实受限请求仍待用户配置控制台白名单并在真机复核。
## 当前执行：26.3 方块兼容与测试反馈

- [ ] F04 以 Minecraft Java 26.3 正式版为目标，官方状态/模型/纹理清单、资源包解析、流体斜面/流向、稳定加权模型、高分辨率混合图集及受限方块实体静态表现已通过单元、扩展 voxel 与 Web renderer 回归。审计见 `docs/research/MC-26.3-resource-pack-audit.md`；完整覆盖仍需按实际问题包继续核对，真机视觉与分发许可仍需人工验收。OptiFine 扩展和逐像素原版对照不在范围内。
- [x] R16 极简空闲信息面板改为面板内直接左右滑动、保留前版高度，去掉底部切换按钮与页码；保留键盘等价操作并隔离纵向选时手势。已补右页指针捕获及越过面板边缘的移动端 E2E，自动化通过，待真机复核。
- [ ] R17 统计页纪念建筑板块增加与成就相同的整体折叠，建筑卡仍独立折叠、默认全收起。代码与定点测试已完成，待真机视觉验收。
- [ ] R18 将稀疏雨滴滑落及短时雷电反馈扩展到极简信息玻璃面板，复用现有天气状态；减弱动态、不可遮挡、低功耗。代码与定点测试已完成，待真机视觉验收。
- [ ] R19 十份补充原生蓝图改用“作者简称：一句视觉描述”；无英文简称的署名 Dieight。建筑目录已更新，奖励蓝图也有同源描述元数据，待下包验收。
- [ ] R20 精简材质包导入说明与纪念建筑无法分配时间说明；现有短文案及必要归因/版权说明已通过定点审计，待真机阅读验收。若仍需长帮助再采用首次进入提示。
- [ ] R21 蓝图库导入的建筑预览适配深色模式，包括加载态、卡片边界与控制层；已补移动端暗色导入定点测试与截图审计，待真机视觉验收。
- [ ] R22 F04 方块解析稳定后扩大普通世界缩放上限，使最大放大尺度略高于导入蓝图／任务建筑预览；同一 8k+ 方块建筑、同尺寸画布下普通世界最近距离 118.5，预览 128.9，约多放大 8.8%，裁剪安全与手势定点测试通过，待真机视觉验收。

## 待执行：验收与收尾

- [ ] 下一轮开始卡顿、通知栏恢复卡顿、OEM 返回专注提醒的真机体验验收。
- [ ] 沉浸重置视角隐藏、选中建筑拖拽的设备/GPU 验收。
- [ ] 轮次汇报、纪念统计、设置数字框补充响应式视觉验收。
- [ ] 整理产品、架构、工作包中已被后续实现取代的旧状态。
- [ ] 2.0.0 正式候选冻结、完整门禁、用户验收与正式发布（发布仍需对应授权）。

旧独立伴侣、跨应用 ContentProvider 身份桥和伴侣安装任务由当前同应用同步方案替代，不继续排期。OriginOS 专属准入/设备适配仍待条件，标准通知回退保持现状。
