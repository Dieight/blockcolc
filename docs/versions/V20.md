# V20 Work Packet

Status: implemented `b22f42a`; refining after first device acceptance (2026-08-17). V20 is the hand-feel and living-world release: the Pomodoro core gets micro-motion, and the world starts breathing.

V20 polishes the timer itself (micro-animations, press feel, transitions) and makes the world feel alive: leaves sway, clouds drift, and buildings assemble block by block after focus rounds and during breaks. No haptics, no UI breathing effects, no agent-facing features (dropped by owner decision).

## Requirements

| ID | Requirement | Status | Evidence |
| --- | --- | --- | --- |
| FX-01 | Timer digits transition lightly on change (fade/slide, ~150-250 ms), instead of hard jumps; respects reduced-motion | implemented | per-digit spans in `FocusTimer` + `timer-digit-rise` keyframes (gated on `prefers-reduced-motion: no-preference`); `v20-ambient-motion.spec.ts` asserts 5 digit spans + `45:00` text |
| FX-02 | Primary buttons (start/end) have press rebound (scale ~0.955) and the start action pulses the dial once; 44 px targets unchanged; respects reduced-motion | implemented | CSS press-rebound block for primary/choice/plan/dialog buttons, springy release bezier, gated on `no-preference`; the existing `playConstructionPulse` (session start 0.6 / completion 1.0) is the dial pulse |
| FX-03 | Focus↔break transitions ease (fade/slide ~200-300 ms) instead of hard swaps; respects reduced-motion | dropped (owner, device acceptance 2026-08-17) | The floating round/break ceremony beat was removed: the existing "材料已送达" construction-feedback already marks a completed round, and the owner found the extra overlay unnecessary and unattractive |
| FX-04 | Pop-in controls (immersive end button, progress report, dialogs, plan sheet, session context) enter with a quiet fade-up in the same visual language as the immersive hint | implemented | shared `ui-fade-in` keyframe (160-200 ms, mount-triggered, gated on `no-preference`) applied to `.immersive-controls .primary.destructive`, `.focus-task-context`, `.rest-summary`, `.focus-integrity-ended`, `.report`, `.confirm-dialog`, `.focus-plan-sheet`, `.habit-building-selection`, `.v7-progress-report` |
| WX-01 | Natural trees sway gently around their trunk axis (cheap instanced trunk-pivot, no vertex shader); disabled under reduced-motion and under the performance lighting tier | implemented, re-tuned | `naturalTreeSway` data + `updateTreeSway` (trunk+crown rigid rotation about the planted base, deterministic per-tree phase); first acceptance round was invisible, so amplitude grew to 2.6-4.3° and period to 6-11 s; gated by `ambientMotionAllowed()` |
| WX-02 | Cloud blocks drift slowly across the sky and wrap per-cloud; disabled under reduced-motion and under the performance tier | implemented, re-tuned | `cloudDrift` data + `updateCloudDrift` (per-cloud sine drift, base positions stored); first acceptance round was invisible, so amplitude grew to 6-12 units (near) / 3-6 (distant) and period to 45-90 s / 60-120 s; gated by `ambientMotionAllowed()` |
| BX-01 | Building growth assembles block by block in blueprint build order at a slow pace (~3-4 s total, each block visible); plays on round completion and continues naturally into the break; reduced-motion falls back to instant growth | implemented | `construction-reveal.ts` (pure diff + wave schedule, unit-tested), reveal plans in `rebuild()`, per-instance zero-scale + underdamped pop in `addBuilding`, `updateConstructionReveals`; skipped under reduced-motion / flat / voidscan / preview; not gated on lighting tier (bounded one-shot ceremony like the pulse) |
| CT-01 | Contract amendment: the world may run a bounded low-cost ambient render loop while the world pane is visible and the app is in the foreground; hidden/background stops frames immediately. The v3 no-continuous-loop assertions are updated to the new semantics (no frames while hidden; ambient frames allowed while visible) | implemented | One throttled interval pump (`updateAmbientMotion`, 100 ms) drives rain + clouds + trees + reveals; `continuousRendering` stays `false` (no continuous rAF loop); new datasets `data-ambient-motion-active` / `data-construction-reveal-count`; `v20-ambient-motion.spec.ts` asserts hidden pane ⇒ gate false, reduced-motion ⇒ gate false, and the no-continuous-loop guarantee |

## Acceptance round 1 (owner, device, 2026-08-17)

- WX-01/WX-02 were not visible on device at any lighting tier → amplitudes/periods re-tuned to readable levels (table above); a conditional E2E now proves idle frames differ while the gate is open.
- FX-03 dropped (unwanted); replaced by FX-04 fade-up for pop-in controls per owner feedback.
- V19 diagnostic cell picking stays available but is now opt-in behind `?pick` (off by default).

## Contract change (approved by owner)

- Old (V3): no continuous render loop; frames only during interaction.
- New (V20): ambient loop allowed while visible + foreground (cloud drift, leaf sway, construction reveals); setVisible(false)/background stops it. Interaction rendering unchanged. Battery: performance tier disables ambient animation; cinematic/balanced use a minimal per-frame update set.

Implementation note: the loop is a 100 ms throttled interval pump (like the existing rain pump), not a 60 fps rAF loop, so `continuousRendering` remains false and idle cost stays minimal while the same visual contract is honored. Ambient renders share the single rAF slot with interaction frames and are adaptively throttled: the next ambient frame is only requested after the previous one finished and an interval elapsed that scales with the measured frame cost (250 ms fast devices → 1 s when a single cinematic frame is expensive), so a slow GPU can never turn the living world into a pipeline-saturating loop (this is what kept Playwright canvas screenshots stable under software WebGL + bloom).

## Data and compatibility

- No state, storage, backup, or import/export changes. The world grows from the same projections as before; only the presentation timing changes.
- The reveal building renders through the per-instance fallback path while revealing; the next rebuild re-batches it into the merged atlas path.

## Non-goals

- No haptics/vibration, no UI "breathing" effects.
- No agent-facing features, no new import/export or sync paths.
- No new world content systems (leaves/clouds/buildings render the same data; only motion is added).

## Open items

- Visual hand-feel acceptance on a real device (sway amplitude, cloud drift speed, pop timing) pending the V20 device build.
- `docs/release-notes-v1.5.0.md` + CHANGELOG entries at release time (version bump requires owner authorization).
