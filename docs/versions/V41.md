# V41 / 2.0.0 收尾发布工作包

## 当前目标

本工作包收口世界天气冷启动提示和 `D:\Litematic` 补充蓝图命名，并准备标准公开 APK 与私有集成 APK。版本名保持 `2.0.0`，versionCode 为 `41`。

## 需求状态

| 编号 | 状态 | 验证边界 |
|---|---|---|
| V-41-01 | 完成 | 世界天气提示显示现实/本地来源，冷启动一次、5 秒显示、180ms 淡出；组件单测与 Web release 回归 |
| V-41-02 | 完成 | `D:\Litematic` 十份文件全部解析，稳定 ID 和建筑/奖励分类保持，最新文件名进入标题/描述署名；打包器校验 |
| V-41-03 | 完成 | 标准/私有 Android 构建入口隔离，标准 APK 不含私有上传模块或配置；构建脚本与 APK isolation |
| V-41-04 | 完成 | 全仓类型、单元、扩展 voxel、存储/core-loop、Web release 门禁完成；失败的环境像素/手势场景已单独重跑通过 |
| V-41-05 | 完成 | 标准发布候选复制、包身份、签名、SHA-256 边界校验完成；用户已确认候选功能 |
| V-41-06 | 完成 | 极简统计区提高浅色高透明玻璃下的文字对比度；统计图采用节奏面积 A 方块阶梯，按小时堆叠实际专注的 15 分钟方格；保留两页双向轮播 |
| V-41-07 | 完成 | 26.3 资源包远景地形改为独立裁剪纹理，避免 LOD 面采样整张图集；计时页重建材质时显示全屏加载；移除加载页底部提示；极简任务色收敛为低饱和绿灰阶 |

## 发布边界

标准候选发布到 GitHub 前必须对应用户实际验收的同一候选 SHA-256。私有集成 APK 不上传，只保留在 `C:\Codex\blockcolc-relay-private\artifacts` 并在交付时报告路径。F04 的完整方块/材质覆盖与真机视觉仍按既有 TODO 保留，不在本收尾切片中宣称完成。
