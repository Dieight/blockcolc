# V27 / 开发工作包

## 2026-09-25 收尾切片：2.0.0（41）天气提示与本机补充蓝图

世界面板天气浮窗现显示当前天气及来源，并保持既有样式/位置；每次冷启动显示一次，5 秒后保留 180ms 淡出动画。现实天气未开启、未定位或请求失败时稳定回退为本地天气；现实天气结果在提示尚未消失时可更新为和风天气。`D:\Litematic` 的 10 个补充蓝图均已按最新文件名解析，稳定 ID、分类、原始描述正文和本地构筑隔离保持不变。

本切片将 `versionCode` 从 40 递增至 41。标准公开 APK 与私有集成 APK 分别构建并做包身份、签名和隔离检查；公开发布候选仍需用户对同一 SHA-256 候选完成正式验收后才可发布。

## 2026-09-25 用户反馈：2.0.0（39）后续返修

本机已安装的私有验证包为 `com.blockcolc.app` 2.0.0 (39)，SHA-256 `3fafae2b3cfed70916c8bfea0fb92c8781b4a021296e34daebb5f67fe43657be`，设备安装证据与源 APK 哈希一致；不是正式发布。2.0.0 (38)/(39) 签名证书 SHA-1 均为 `51:04:B0:56:70:7B:D6:C5:D1:F0:8C:2F:76:B5:5C:85:C2:84:9F:F1`。

2026-09-25 收尾验证：R16 极简今日页右滑返回、R10 现实天气定位/缓存/DTO 错误边界及 R23 三张内置蓝图补玻璃和内饰均通过定点单测与 Web release 回归；快速门禁、扩展 voxel、Web release（175 passed、3 skipped）均通过。按已确认授权将 versionCode 递增至 40，并从同一工作树构建本地私有签名验证 APK：`C:\Codex\tomato-clock\artifacts\verification\v2.0.0\Blockcolc-v2.0.0-verification.apk`，包名 `com.blockcolc.app`，版本 `2.0.0 (40)`，SHA-256 `8d0d9c3029db3779fa9f04c5b753ed164770df48e2bd7640876b84f8c340f1e9`，证书 SHA-1 `51:04:B0:56:70:7B:D6:C5:D1:F0:8C:2F:76:B5:5C:85:C2:84:9F:F1`。未上传、未安装；无连接设备，真机视觉与和风天气白名单请求仍待用户人工验收。本包不是正式发布。

- R16：已修复今日页右滑回时钟的指针捕获边界，保留面板高度、纵向选时和键盘等价操作；定点与 Web release 回归通过，待真机复核。
- R10：已完成定位、原生请求、2xx 解析、桥接及 UI 错误映射；仅本机缓存上次有效坐标，定位失败时用缓存重试，设置如实说明缓存。定点与 Android 构建门禁通过，真实受限请求待用户配置控制台应用限制后真机复核。
- 新增三张原始内置蓝图视觉返修：空窗补玻璃并适度添加内饰；复核材质、透明度、施工顺序及旧任务/导入蓝图边界。

上述返修的定点与集成检查已完成；不继承旧包测试结论，也不据此宣称现实天气已在真机成功。真机视觉、定位授权、控制台应用限制和真实受限请求仍是人工门禁。
用户已明确授权：上述三处返修通过定点验证后，将构建号从 39 递增至 40，制作同包名、同签名的私有验证 APK 并覆盖安装当前连接手机，保留用户数据；本轮构筑已完成但无连接设备，未安装，视觉与天气效果仍由用户人工验收。

## 2026-09-24 验证构筑：2.0.0（38）

本轮将 `D:\Litematic` 新增的“GYP的简易小仓库”列为普通建筑、“神秘附魔台”列为每日奖励；十份本机蓝图合计七份普通建筑、三份每日奖励，按稳定 ID 逐一解析/打包，原始 Litematic 与生成资源仍不进入公开源码。Minecraft Java 26.3 本机客户端 JAR 与对应 DataGen 审计覆盖 1,286 方块、35,723 状态；水浸 `waterlogged=true` 另独立审计 13,325 状态，隔离规划无遗漏。四个用户样例叠加本机 26.3 底包，含水真实蓝图的移动端资源包用例通过；详见 `docs/research/MC-26.3-resource-pack-audit.md`，不把静态路由覆盖写成方块实体/NBT 与流体逐像素一致。

`npm run test:unit:full`、官方覆盖审计 7/7、Web release 176 项完整运行（174 passed、2 skipped）、私有集成版 Android `testDebugUnitTest lintRelease assembleRelease` 均退出码 0。签名私有验证 APK 为 `C:\Codex\blockcolc-relay-private\artifacts\integrated-20260924-133246\Blockcolc-v2.0.0-private-verification.apk`，包名 `com.blockcolc.app`，版本 `2.0.0 (38)`，SHA-256 `2680e3576143b26be48548071690e620cf15854e391fe82556ff249ad50b4593`，证书 SHA-1 `51:04:B0:56:70:7B:D6:C5:D1:F0:8C:2F:76:B5:5C:85:C2:84:9F:F1`。已用 `adb install -r` 覆盖安装 PJX110，并核对设备 base.apk SHA-256 完全一致，未清数据；未进行安装后 UI 巡检，也未上传测试仓库（设备已连接）。仅供用户人工测试，不是正式发布。

## 当前优先级（2026-09-21）

2026-09-23 后续切片：用户恢复 F04，官方 26.3 已于 09-15 正式发布，故目标固定为 Java 26.3 正式版。先生成上游方块/状态/模型/纹理覆盖清单，再按 ZIP 导入→解析/路由→atlas/tint/几何→场景采用的失败边界修复，未覆盖的方块明确标记，不宣称安全回退即完全对应。同步返修极简空闲信息面板：取消底部切换按钮与页码、保留上个版本面板高度并只在面板上左右滑动，键盘提供等价路径；纪念建筑统计外层整体可折叠。极简面板天气为受控视觉增强，晴天不持续调度，雨滴稀疏滑落、雷暴短时反馈，减少动态时静止或隐藏。2.0.0/37 旧 APK 的体验验收等待用户反馈，本切片不擅自构建新验证包。

2026-09-23 下一轮验证构筑已将 versionCode 由 36 递增至 37，保持 `2.0.0`、原包名和签名。全量类型/单元/扩展地形测试 exit 0；Web release 分类 173 项完整运行，171 passed、2 skipped、exit 0。私有集成版 Android `testDebugUnitTest lintRelease assembleRelease` 退出码 0，签名档案 `C:\Codex\blockcolc-relay-private\artifacts\integrated-20260923-190610\Blockcolc-v2.0.0-private-verification.apk` 的 SHA-256 为 `acc2bca7219bd5e779b7d080744ea34a484b7850b4a388efc3bc91236ab8edb3`，包名 `com.blockcolc.app`、`2.0.0 (37)`，证书 SHA-1 为 `51:04:B0:56:70:7B:D6:C5:D1:F0:8C:2F:76:B5:5C:85:C2:84:9F:F1`；八份本机蓝图 ID 均在 APK Web assets 内。ADB 无设备；向私有 `Dieight/blockcolc-test` 的 APK-only 上传曾返回 HTTP 401，其后远端只读请求又出现 TLS 超时，目前未取得远端回读哈希，不宣称完成远端交付或用户真机验收。此包不是正式发布。

新增本机蓝图切片：`D:\Litematic` 八份蓝图打入下一测试构筑，其中 `WQH的小黄鸭` 与 `小水箱` 走每日奖励资源，其余六份走普通建筑目录。复用现有解析/校验/世界预览与奖励事实；检查奖励尺寸预算、幂等注册、备份恢复及包体。六份本机建筑用 `builtin-local-*` 稳定 ID，创建任务及习惯下一座建筑时保存完整 `importedBlueprint` 快照，确保后续不带可选素材的构筑仍可还原；奖励资源由领域命令幂等存入本地状态，旧用户资源不被覆盖。原始文件和生成内容资产不进入公开源码；正式发布前核对分发许可。全量测试在该切片集成后再跑。

2026-09-23 现实天气决定覆盖 09-21 的“两个版本都仅自填”规则：用户授权从本机排除文件读取 Host/API KEY，于 Android 标准与私有测试构筑原生层注入；Web 不带凭据，设置仍默认关闭。Android REST 请求带包名和签名证书 SHA-1 头，用户在版本完成后把实际测试 APK 对应身份加入和风控制台。APK 内凭据可被逆向提取，限制头可仿冒；不夸大为不可盗用。须核对实际签名 APK 的包名、证书 SHA-1 与新包哈希，待用户加白名单后才可做 Android 限制下真实 API 验证。请勿把凭据、主机或原始响应写入测试日志/公开源码。

现实天气本地配置已收到，用户随后补齐 API Host；Host/Key 已通过不回显原值的格式检查，文件已加入本机 Git 排除。自动连续专注已确认包含锁屏/后台，不能以仅前台定时替代。

09-21 曾计划让两种构筑都由用户自填，已被顶部 09-23 Android 原生构筑注入决定覆盖。官方实时天气现为 `/weather/v1/current/{latitude}/{longitude}`，支持 `X-QW-Api-Key` 请求头；Host 必须为账户专属域名。对接以当前官方文档为准，不使用旧共享域名或把 Key 放入 URL。用户设置控制台白名单前不能将真实受限请求标记为验证通过。

R09 图表选型：完成 L1–L15/F1–F13 扫描，L10 径向事件分布不符合用户明确横向 00–24 时间轴，L3 单色日条码不能表达任务构成，F7 仅少量分类堆叠不适合全天 96 桶；沿用 F3 Hairline Area (`basics-gallery.html` / `Concurrent users, filled with days`) 的发丝区间、基线、轮廓和逐列淡入，按参考图扩展为任务分色的 15 分钟区间线段，禁止平滑填补空档。每个已归属小任务独立配色，未归属时间独立灰色，不合并成“其他”。投影 6 项单测及横滑规则 2 项单测通过；固定玻璃面板、左右内容切换及纵向选时隔离的移动端 E2E 1/1 通过，仍待用户真机视觉验收。此面板时长按日历时区真实当日区间，完成轮次沿用完成日/每日目标契约，底注解释跨日差异。

R04 蓝图库导入候选复用新建任务的 `BlueprintPreview`，共用资源包、旋转和重置视角；真实本地 `.litematic` 导入→候选预览→保存/重命名→再选用的移动端定点 E2E 1/1 通过，预览截图已检查。小预览云块仍过大，作为 F08 的独立 renderer 返修，不把 R04 的组件接线误记成 F08 完成。

R01 增量检查点：恢复流程新增可选 `StateRepository.loadIfChanged`，每个异步通知边界仍读取实际持久化修订，未变时跳过共享 schema 解析及额外克隆，不取消并发替换/清空检查。IndexedDB 本身的记录读取与结构化克隆仍存在，尚无真机手势延迟提升结论。应用层定点 56 项、存储 31 项与两包 typecheck 均 exit 0；测试明确验证三次未变读取不重复调用解析器、外部提交可见以及原生能力等待期间替换会取消旧通知。不是整轮完成或 APK 门禁。

历史执行分工已由当前工作树整合；R11–R15、F04 及本轮 R16/R23 的自动化与本地验证构筑已完成。共享构建/Playwright 输出已串行完成；仍保留 R01–R08、R17–R22 的人工设备验收，以及现实天气控制台白名单和真实受限请求验收，不把这些门禁提前标记为通过。

2026-09-20 备份/升级返修已交付：构建号 35→36，同包名、同签名比对通过；备份入口和共享解析上限统一为 100 MiB，存储 29 项、Web 文件/同步 11 项及类型检查通过，签名构建与最终私有 APK 权限门禁通过。APK-only 上传到私有测试仓库，回读 SHA-256 `a03b0d352827724925e821d66499a6de3d102dc0d5ef37de50a4edca6f5c5043` 一致。未连接手机，仍需用户验收覆盖安装与真实备份。查看邀请补多设备独立链接，保留七天会话；精确部署状态见同步工作包。

2026-09-20 同步返修已交付：旧验证包缺少网络约束 JobScheduler 所需 ACCESS_NETWORK_STATE，且吞掉调度失败，手机上传未通过。已补私有 manifest、失败回报/重试与最终 APK 权限门禁；Web 同步 5 项、私有原生 33 项和标准原生 20 项、两构筑 lint/签名构建通过。新 APK 已单独上传私有测试仓库并回读比较 SHA-256，手机未连接，未安装；真实手机上传仍待用户验收。精确产物见同步工作包。

2026-09-20 用户确认：当前私有同步切片收尾后单独交付测试 APK，不等待暂停的 UI/世界/材质工作；不是正式发布。部署与测试证据汇总到同步工作包。

交付完成：私有同步测试包 2.0.0/35 已覆盖安装 PJX110，设备/归档 SHA-256 一致；服务器已上线。用户后续连接手机，未上传测试仓库，未做安装后 UI 巡检。最终私有/标准 APK 隔离检查、28/20 原生单测及 release lint/构建通过；服务 32 项和实际公网 SSE 通过。详见 `docs/handoffs/BLOCKCOLC-RELAY-PLAN.md`。网页汇总热力图改为每 2 小时一档，App 不改。真机端到端与后台体验仍由用户验收，不将此验证包作为正式发布候选。

用户要求暂停既有未完项，统一迁入 `docs/TODO.md`。当前只推进好友服务器同步：标准版与私有同步版共用核心，私有版直接集成上传模块，取消独立伴侣 APK。方案和实际结果见 `docs/handoffs/BLOCKCOLC-RELAY-PLAN.md`。用户随后更新部署授权，2026-09-19 固定主机指纹 status 查询成功，凭据至2026-10-20有效；本地验证通过后继续部署。下方 UI/非材质交付为历史检查点。

## 当前入口：2.0.0 非材质项完整测试构筑（2026-09-13）

用户最新要求：首版 UI 已安装并收到设置单位返工；本次不再单独出 UI 小包，完成 F04 材质兼容之外的全部后续项后，统一测试、构建、安装。完整范围、已确认总时长/成就口径与独占文件分工见 [V27-2.0-FOLLOWUP.md](V27-2.0-FOLLOWUP.md)。完成事件驱动等待，安装后交用户人工检查。

### 已完成的首批测试包（历史）

以 [V27-2.0-FOLLOWUP.md](V27-2.0-FOLLOWUP.md) 为实施与验收入口。准备已完成，用户确认简单 UI 优先，先交付 F01/F03/F09/F10/F12 的 verification APK，再处理复杂功能。当前保持 2.0.0/35；保留其他代理的新构筑。Astra 规划/审计，Luna 分工执行，不把下方旧通过记录转作新功能证据，不授权正式发布。

首批代码已实现，2026-09-13 受影响回归完成，正式签名测试 APK 已覆盖安装到 PJX110，待用户反馈；新增 F19/F20 归属与纪念统计仍待后续切片。精确通过/失败/设备及 APK 哈希记录见当前工作包，不代表正式发布验收。

## 当前 UI 重设计（2026-09-09，2.0.0/34）

用户要求本轮由当前代理直接完成，不再交接或委派。覆盖下方旧 UI 所有权与极简开始按钮方案；保留既有核心重构、用户数据与历史验收记录，不增加版本、APK 或发布授权。

| 需求 | 确认范围与验收 | 状态 |
| --- | --- | --- |
| UI-27-R1 | 计时页极简/任务入口相邻；各专注模式共用材料、几何与计时组件；活动面板移除建筑完成度 | 实施中 |
| MINI-27-R2 | 极简空闲常态仅当前时间，上下滑动选择结束点，双击所选时间开始；休息仅倒计时，双击跳过并开始下一轮；空白双击只切换返回完整模式 | 实施中 |
| ASSET-27-R3 | 四时段（06–12、12–18、18–24、00–06）不规则色块重构表盘，番茄外框简化线条；通知镂空图标保持 | 草案讨论中，正式资源未替换 |
| STATS-27-R4 | 常看统计前置、删除重复指标、成就下移默认折叠；从 lieflat-charts 真实模板选型并适配离线 React | 已实现，待完整门禁及真机验收 |
| TASKS-27-R5 | 任务层级、统一组件及细节打磨，保留 IME、排序、目标与删除确认边界 | 已实现，待完整门禁及真机验收 |

手势验收包括单击不启动、拖动不触发双击、键盘等价路径、午夜日期可辨、过近/过期结束时间拒绝、重复提交、持久化失败、休息结束与恢复、最终统一汇报。滑动草稿不写入计时事实；只在明确确认时使用既有 starter。完整 Web 门禁与五视口浅深视觉验收仍需本轮新证据，不能继承 R1–R5 旧测试结果。

### 2026-09-09 当前 UI 测试安装

本次安装后收到用户五项返修及图标再生成要求，当前按下表继续实施；旧通过记录不作为返修验收。

| 需求 | 返修验收 | 状态 |
| --- | --- | --- |
| UI-FB-01 | 上下滑回当前时间清除结束草稿，恢复实时时钟；拖回原点、跨分钟与键盘等价路径 | 实施中 |
| UI-FB-02 | 中断只中断当前轮，保留计划；待继续显示剩余总时长；退出完整界面后可重新进入极简 | 实施中 |
| UI-FB-03 | 恢复应用绿色/浅深配色；固定日历不位于时间范围控件作用区；直接点选日期并可键盘查询 | 实施中 |
| UI-FB-04 | 今日目标、任务详情、总览与管理统一背景、字体层级和触控；去除蓝色触控高亮 | 实施中 |
| UI-FB-05 | 集中区分可调沉浸玻璃、固定阅读玻璃、固定功能玻璃及实底；滑杆两端检验各消费者 | 实施中 |
| UI-FB-06 | 内置 imagegen 再生成四时段无指针刻度图标供预览，不自动替换正式资源 | 草案已生成，待用户反馈 |

本轮用户明确恢复应用配色和交互，优先于下方初版 lieflat Mono 选型约束。仅保留诚实的数据比例及经选择的模板结构；不再把模板字体、灰阶背景、英文来源标签和日期输入器当作应用规范。

用户明确“设备连上了，推”，授权将当前改动构建并安装到连接设备，不包含 Git 推送或正式发布。版本继续 2.0.0/34，旧正式图标保持。全工作区版本、类型、单元测试通过（Web 186 项）；定向业务/统计 E2E 25 passed。新增五视口浅深主题交互与布局检查 10 passed，已查看部分截图，尚未完成全部人工视觉检查。休息恢复用例首次在并行 Android Web 构建替换测试 dist 时失败，独立重跑 1 passed（10.8s，exit 0）；不将分次结果声称为冻结树完整门禁。

`Build-AndroidRelease.ps1` 正常路径运行，真实 fixture 哈希比较、Web typecheck/build、Android testDebugUnitTest、lintRelease、assembleRelease 成功，Gradle 1m53s。测试包 `artifacts/test-builds/ui-redesign-20260909-1330/blockcolc-2.0.0-34-ui.apk`，4,154,895 bytes；SHA-256 `976c0eb4d269e1f380b55113d0963322a3719c77c9db4ffc432f7de82f5b013d`。包名、2.0.0/34 和正式签名证书均与受控配置比较一致；构建输出到归档、安装前、设备 base.apk 三边界 SHA-256 比较一致。已通过 `Install-AndVerifyApk` 以 `adb install -r` 安装到 PJX110 `a50247d1` 并启动，未清数据、未卸载、未提交/推送/发布。此包仅供本轮真机反馈，不是已接受的正式候选。

### 统计模板审计（STATS-27-R4）

数据审计：现有只读投影提供按冻结本地日聚合的实际分钟、有效完成轮次、活跃日和项目实际分钟。中断前实际时间仍保留，不新增事实或改变归属。首部统一今天/7/30/90 天范围，只显示时长、活跃日、有效完成三项；删去重复本周/近期摘要、完成率、时段项目堆叠和常驻中断原因。长期日历与任务分配分别回答持续投入和投入去向，成就末尾默认折叠。

已完整扫描 catalog 的 L1–L15、F1–F13。日历候选：L3 Barcode Lollipop 可承载日序列但 182 根在手机难以逐日查询且丢失星期结构；L4 Arc Matrix 超过轻量矩阵的 100 格范围；L9 Bubble Almanac 的多变量旁注对单一日分钟过重；F2 Hairline Line 保留走势但不保留星期模式；选 F10 Dot Heat。项目候选：L2 Dot Cascade 要求暗卡/短竖排标签，不适合长任务名；F1 Rung Bars 同样挤压横轴标签；F4 Tick Donut 把实际时长变成占比，弱化直接比较；选 F5 Tick Rows。未使用 Glance。

结构来源均为 `lieflat-charts/templates/basics-gallery.html`：F10 卡内标题 `When support gets loud` / C6 dot heat；F5 `Six teams, shipped and counted` / C1 tick rows。沿用前者星期行、时间列、面积开方、静默小点和峰值虚线圈；后者横向队列、发丝刻度、每五刻点标、末端数值及逐行/逐刻淡入。适配 26 周日历和真实分钟尺度，刻距单位明确显示，长中文标签移到行首可换行；不虚构个体记录。图表全部锁定 Mono 灰阶、实底无阴影，明暗主题只反转同一灰阶语义。沿用应用离线字体栈，以 Inter 为可用首选，不加载 CDN 字体。键盘/触控可通过独立 44px 日期控件读明细，不依赖细小点的 hover。

## 核心重构进行中（2026-09-08，2.0.0/34）

用户明确暂缓 UI 返修与伴侣，直接推进核心重构；保留最新 UI。2026-09-09 收尾复核：R1～R5 已完成，轮次/命令/生命周期消费/专注流程/渲染宿主/手势及主要展示组件已分离。全仓类型和版本检查、413 项相关单测通过；最终冻结树完整 Web 120 passed / 2 既有 skipped、799.5 秒、exit 0，源码指纹前后比较一致。首轮旧设置断言失败已按确认的隐藏规则修正并完整重跑，设置实现不变。无性能 A/B 提升结论，无 APK/提交/发布。详情以 `docs/handoffs/V27-CORE-RESULTS.md` 为准，覆盖下方旧的实施优先级和未接线记录。

### 当前核心状态

| 需求 | 当前状态 |
| --- | --- |
| ARCH-27-01 | 本轮 R1～R5 完成，最终冻结树 Web 验证通过 |
| PERF-27-01 | 驻留/重建不变量通过；完整交错 A/B 未做，R6 不触发 |
| MINI-27-01 / ACHIEVEMENT-27-01 | 已接线，当前 Web 主流程与恢复回归通过；不替代 UI/真机最终验收 |
| RECOVERY-27-01 / ORIGINOS-27-01 | OnePlus 提示真机验收及 OriginOS 外部前置仍开放 |
| DOC-27-01 | 本轮架构/重构结果已同步，未将 V27 其他历史遗留标为完成 |

## 最新按钮方案交接（2026-09-07，2.0.0/32）

用户接受“半透明森林绿玻璃开始按钮”：浅色淡绿底深绿字、深色深绿底浅白绿字，轻微边缘高光/模糊，无发光或循环动画；滑杆联动但保留材料与文字可读性。仅确认与交接，尚未实现。具体CSS级联原因、局部修改边界和验收见docs/handoffs/V27-MINIMAL-START-BUTTON.md。接手复测types/117单测exit0，源码定向E2E17pass/1既有skip、2.5分钟exit0；无APK构建安装，非完整发布门禁。

## 最新接力覆盖（2026-09-07 DF-A2）

用户要求先审UI、完整交接V27全部剩余工作，再继续伴侣/部署。当前代码2.0.0/29；DF-A2 Web types/118单测/版本通过，独立五视口浅深92/92、七日字号命中补测通过，源码E2E12pass/1既有skip exit0。新增DF-A2-01：普通/习惯99:99仍允许确认23:59，需UI返修。ADB为空，无本次APK安装/正式发布。详见docs/handoffs/V27-DF-AUDIT.md。

**后续统一从 docs/handoffs/V27-REMAINING-HANDOFF.md 开始**：包含全部需求状态、R0～R6重构、UI/核心/真机/OriginOS外部前置、正式发布顺序，以及伴侣S1～S10逐步接续。原生只读桥/Kotlin同步已有未验证草稿；服务器只读查到既有占位页，未env/deploy。下文旧未接线/旧版本/无代码状态不覆盖本入口。

## 2026-09-07 私有伴侣核心切片 1

先完成DF-A1审计并回写后，继续隔离核心：主树纯focus-export投影新增8测试，application typecheck与62/62通过；私有目录协议/日总量SQLite/内存实时态/鉴权/HTTP/SSE基础，typecheck与13/13通过。没有App接线、原生桥、Kotlin伴侣或网页实现；无部署/构建APK/安装。详情见BLOCKCOLC-RELAY-PLAN.md顶部，不把基础切片当作整项完成；UI仍拥有返修文件。

## 2026-09-07 真机反馈返修独立审计 DF-A1

实际主目录 dirty 源码审计未通过：typecheck 8 条 TS18047；Web 117/117 单测通过；独立源码模式 E2E 12 passed/1既有skipped（1.9分钟）；五视口×浅深探针62PASS/30FAIL（3类问题重复）。加360/412字号/裁切补测，共5项返修详见 docs/handoffs/V27-DF-AUDIT.md。关键九文件前后哈希相同，无ADB设备，不构建/安装/发布。核心先完成本审计并同步交接，再继续隔离的伴侣核心。

## 2026-09-07 私有伴侣与专注看板方案

后续确认覆盖下文旧“补传待冻结”：用户已同意日总量补传；网页首选新状态到达后的 SSE 推送，不支持时前台每五分钟查询。两次同步间本地估算并显示手机状态年龄，保活/同版本查询不刷新该时间；同步年龄不等同断线。撤销旧五秒轮询/90秒过期建议，详见方案第 3–4 节。仍仅文档方案更新，未实现或部署。

用户确认本体只读出口 + 私有伴侣上传 + 服务器日汇总/热力图，网站采用独立 Brutalism / Swiss 视觉；不让其他用户向个人服务器上传。调研、数据边界、资源目标和核心/UI 任务表见 `docs/handoffs/BLOCKCOLC-RELAY-PLAN.md`。当前仅方案，不是已实现能力，也不扩大本版本必须发布范围；当前真机 UI 返修优先。五字段之外的离线日汇总补传、历史范围和无小任务习惯命名仍需协议冻结。本轮未构建、测试、部署或安装。

## 2026-09-07 核心接力方案

按用户要求完成 `docs/handoffs/V27-CORE-REFACTOR-PLAN.md`：R0 基线、R1 纯视图合同、R2 轮次上下文、R3 命令/生命周期消费、R4 专注流程、R5 壳层/渲染宿主、R6 测量后选做优化。当前只交方案，未实施新重构、未重跑测试；UI 真机返修仍优先。保留领域/存储/渲染底层及 schema12，App.tsx 单一负责人，接手必须从最新 dirty tree 而非旧 HEAD 开始。

## 当前真机反馈返修（2026-09-06）

DF-CORE-01 定向完成：schema 12 严格迁移；秒阈值 1..60/default 3，旧命令省略时保留值、严格大于计数、豁免/完成优先不变。全仓类型与版本检查 exit 0；domain 144/application 54/storage 27/Web 117 单测通过，真实存储 E2E 1/1（5.8 秒）、production 专注回归/中断提示 4/4（1.2 分钟）exit 0。DF-UI-01～09 和设置输入接线待 UI；未跑本轮完整发布门禁，未构建/安装。PJX110 在线且已安装 2.0.0/27。完整接口/所有权/验证见真机反馈交接顶部。

用户确认已在 UI agent 完成的测试构筑上试用；下方旧入口待接线状态不再描述当前阶段。DF-UI-01～09 委托 UI 按 `docs/handoffs/V27-DEVICE-FEEDBACK.md` 修改；DF-CORE-01 核心实现可配置离开秒数与 schema 12 迁移。当前返修未验收，不复用旧构筑测试作为新源码证据。

> 状态：UI A4 交互待返修；极简核心连续性/统一汇报已接线并验证，首次启动/设置/沉浸展示入口待 UI 集成
> 建立日期：2026-09-05
> 当前用户授权测试候选：`2.0.0` / versionCode `27`，正式签名；完成极简/成就集成与完整流程验证后构建，不授权线上发布

## 版本主题

### 最新审计覆盖旧状态（2026-09-06，A5）

UI 冻结提交 `2c4d81ffd310cfcdff598f7ba67af85056571acf`：Web typecheck、57/57 单测、定向生产 E2E 10/10（1.2 分钟）均 exit 0。独立极简探针 170 PASS / 10 FAIL、exit 1，剩余 A5-01：busy 恢复后 Shift+Tab 从容器逃到背景，Space 可实际改变背景偏好。输入/空值/大字号控件返修可保留，成就只读 props 与极简偏好纯展示行可保留；入口继续待验收接线。详见交接顶部。核心 296 单测/30 E2E 检查点已经验证；下方 A4“尚未验证”仅为历史状态。本轮不合并、不发布，ADB 无设备。

### 历史审计（2026-09-06，A4）

UI 冻结提交 `20844b64d83aaf3e2a5cc300716c9a1375902a24`：Web typecheck、47/47 单测、既有定向 E2E 10/10（32.6 秒）均 exit 0；极简独立交互 10 组实测仍发现输入重渲染抢焦点、空串变 0、busy Tab 穿到背景和 200% 根字号横屏裁切。MINI-27-01 继续“展示返修/核心接线中”，不宣称可用；A4-01～05 和两项纯展示小任务见 `docs/handoffs/V27-UI-AGENT.md` 顶部。UI-SMALL-01 为统计页只读成就 props/移除 DEV 假数据；UI-SMALL-02 为极简偏好行纯组件，核心保留 App 与存储/计时所有权。本轮不合并 UI，不构建/安装/发布；ADB 无设备。主树暂停中的新核心接线改动尚未验证，不继承旧测试记录。


V27 延续 V26，以统一体验与工程边界为主题：收口已知回归，按状态所有权拆分应用编排，优化实际计算和更新成本，统一 UI 与视觉资产规范，并清理文档／测试漂移。保持技术栈、既有业务语义和备份真相。

设置页、返回键、toast 动作与微动效已有初稿，不能把它们等同于 V27 全部完成。用户于 2026-09-05 确认在适合的场景实际使用液态玻璃；UI agent 的任务、文件所有权与验收见 `docs/handoffs/V27-UI-AGENT.md`。UI 现按 A1 审计返修，用户已授权核心并行重构；每轮审计必须回写交接文件。当前不是 APK 构建或发布阶段。

## 明确不做

- 新功能限轻量成就、iQOO 原子岛适配及极简模式。为用户确认的无小任务/末尾统一结算新增 schema 11 显式标识及旧资料迁移，不增加奖励经济或云依赖；UI agent 不改领域命令、事件或存储事实。
- 不引入 UI 组件库、路由库或第三方动效库；继续维护极简依赖。
- 不把所有页面强制换成玻璃卡；世界悬浮层和合适的菜单／Sheet 使用玻璃，设置采用轻玻璃，内容／图表／表单按可读性选择。沉浸几何、滑杆材料映射和本阶段的实底导航保持稳定。
- 不新增环境、Shader、资源包兼容范围或 ColorOS 私有接口。核心 agent 可为已确认的恢复／性能问题调整内部实现；UI agent 不修改原生插件或渲染管线。

## 需求与验收

### UI-27-01：设置页 Apple 分组列表 + 液态玻璃

- 设置采用 iOS 分组列表：分组标题在卡外，行收纳进 `settings-list` 圆角轻玻璃卡；18px 圆角可保留，白色覆盖、反光与软投影按实际背景收敛，避免厚白卡和叠片边缘。材质 token 浅／深两套，参数不以初稿 blur／描边数值作为不可修改的验收条件。
- 行名、说明与分组层级保持一致；触控命中区域至少 44×44 CSS px，焦点可见，字号放大后可重排。
- 文字选项组升级为 iOS 分段控件（凹槽轨道 + 浮起实底选中片，取消绿色下划线）；`switch-control` 在设置内升级为 iOS 药丸开关（`ios-switch`，51x31、绿色 checked）；数字输入改为浅色凹槽字段。
- 新增"高级"分组，"显示世界坐标"从"世界"组迁入；其余设置内容与 aria/role 名称不变。
- 计划专注日：行宽足够时保持 44px 圆形，窄屏允许换行／重排；不能只保持高度而将命中宽度压到 44px 以下。返修 360px 初稿约 37×44px 的实际按钮与只检查高度的测试。
- 三个数据面板（建筑蓝图库、方块材质包、本地备份）加入同一玻璃卡材质；`prefers-reduced-transparency` 时全部回退实底。
- `settings-layout.spec.ts` 验证真实命中尺寸与不重叠、选中状态、焦点、重排及最终减少透明度回退；增加三个数据面板后置样式覆盖回退的回归。视觉截图与行为证据共同验收，不能只断言存在 blur 和圆角。

验收：`settings-layout.spec.ts` mobile/desktop 双视口通过；`v23-appearance`（毛玻璃滑杆不变量、深色切换）、`world-coordinate-setting`（高级分组内开关持久化）通过。

### UI-27-02：Android 返回键逐层退出

- 建立 `back-layer.ts` 分层栈：可关闭覆盖层（结束专注对话框、计划单、沉浸结束按钮、建筑记忆、关于、落成仪式、创建草稿）在可见时各注册一层，返回手势自顶向下派发，任一层消费即止。
- App 根层订阅原生返回（复用 `platform-capacitor` 既有端口）：栈空时非 world 页签先回 world，创建流程先取消，world 空栈才 `exitAndroidApp`。
- 单元测试覆盖栈序、穿透与重复注销；Web 平台不订阅（端口自身 no-op）。

验收：`back-layer.test.ts` 通过；全仓类型检查与 Capacitor 测试通过；真机返回键路径在候选阶段人工复核。

### UI-27-03：可操作 toast 与安静动效

- 消息状态升级为 `{ text, action? }`：通知能力降级/不可用类警告附带"去设置"动作，点击导航到设置页；其余确认型消息保持纯文本、指针穿透。
- 有效离开警示不经过全局 toast（维持 V26 的世界页五秒警示片契约）。
- toast 入场动画与深浅主题切换过渡只在 `prefers-reduced-motion: no-preference` 下启用。

验收：`v5-focus-flow`（toast 生命周期）、`focus-regressions`、`v22-integrity-ended` 通过。

### UI-27-04：沉浸计时建筑语义与启动/主题微动效

- 活动专注带新增"建筑 · 施工阶段 + 完成百分比"静默标注，绝对定位在带底部内边距区，不参与计时器居中布局（V16 居中不变量保持）。
- 启动页方块改为依次升起落定的铺设动画（1.2s 循环），`prefers-reduced-motion` 下静态显示。
- 深浅主题切换对壳层颜色做 0.24s 过渡，同样受 reduced-motion 约束。

验收：`focus-regressions`（计时器居中）、`v20/v21/v22`、`responsive-qa` 通过。

### UI-27-05～08：组件、页面、视觉资产与验收

- UI-27-05：按真实重复点提取共用展示组件，统一语义 token 与全状态，迁移并清理 `foundation.css`／`theme.css` 的对应旧规则，不新增版本式覆盖文件。
- UI-27-06：统一设置、任务／创建、统计、计时、计划／汇报、建筑记忆和提示的表现；保持业务含义与布局特例。App 内新增 JSX 接线归核心 agent，UI 提供独立展示组件与明确待接线清单。
- UI-27-07：规范 UI 图标、材料与视觉资产来源／许可、母版／派生关系和校验；沿用已有生成基础，不替换已验收原生通知图标或改写用户资产。
- UI-27-08：完成五视口、浅深主题、触控、IME、焦点、减少动态／透明度与受影响回归，交付 `docs/handoffs/V27-UI-RESULTS.md`。未验证与待接线项显式标记。

详见 UI 交接文档；它定义实施边界，不代表上述工作已经实现。

### RECOVERY-27-01：退出提示与流程契约（核心）

- 定位并修复用户最后确认的“有效离开计数正确，但旧式五秒提示不弹”；沿用正确计数与旧提示风格，不另加全局 toast。
- 用真机证据贯通原生注意力事件、领域结算、一次性回执和可见 UI；普通／习惯、固定／马拉松、正常／提前／中断形成共享验收矩阵。

验收：定向自动化与 OnePlus 实际返回提示均通过；旧 V26 自动化记录不替代此次真机验证。

### ARCH-27-01：状态所有权与应用编排（核心）

- 逐项提取应用壳、专注流程／视图、汇报、世界适配、偏好／提示；清理确认不可达的旧路径。维持权威时间戳、CAS 持久化和既有命令语义。
- 核验创建／取消任务与恢复路径的页面驻留，防止子树替换重新引入状态丢失和初始化等待。

验收：接口与所有权明确，迁移前后核心流程一致，页面／世界初始化与一次性事件无回退；文件行数不作为完成标准。

### PERF-27-01：实际更新成本（核心）

- 沿用 V26 首帧、全部主页面就绪、世界重建与发布计时基础，测量统计聚合、马拉松更新与计时展示的真实成本，按状态依赖缩小刷新范围。
- 评估基于现有缓存快照的订阅边界，隐藏页面停止展示性工作；不把成本延后到首次点击。

验收：同机同数据交错 A/B，报告启动、首帧、热切换、交互帧、内存和后台工作；未经测量不承诺倍数提升。最终 V26 发布证据的 Web 门禁为 777.6 秒，此前约 9 分钟是不同开发树记录。

### ASSET-27-01 / TEST-27-01 / DOC-27-01：资产、验证与规范（核心／集成）

- 资产生成、输出校验与稳定标识基于现有母版、脚本和内容寻址完善，原生资源与用户蓝图变更由核心／集成负责。
- 优化最慢验证的重复准备，沿用验证 APK／正式发布两阶段，不削减当前行为契约或拼接不同树的发布证据。
- 当前规则直接维护产品事实、设计和架构；历史实施记录放版本工作包，清理材料、几何与发布状态的矛盾。

### MINI-27-01：极简模式（统一结算已确认，展示/核心并行）

- 设置本机开关，启动直接进入既有沉浸几何；空闲大数字显示 HH:mm，专注/休息显示真实剩余时间。无需小任务/轮次选择，唯一主入口“开始专注”打开应用风格的“专注到”结束时间 Sheet。
- 保留返回完整界面的明确次级动作；临时返回不取消计时、不暗改持久偏好。活动计时、待汇报、恢复失败优先；空闲时钟仅前台按分钟更新，后台不启动新计时循环。
- 复用 marathonEndInstant 与 planRoundsForDuration、既有 StartFocus/endsAt/通知，不另造排程或第二份倒计时。时间草稿显式显示今天/明天，取消不改计划；新开始不能覆盖既有运行或待汇报计划。
- 用户确认不绑定小任务、全部完成后统一提交进度，复用 ReportMarathonFocus；不逐轮推进习惯建筑。核心保留宿主项目用于兼容历史关联，不替用户选择小任务；deferredSettlement=true 只允许 marathon=true 且 subtaskId=null。旧记录不推断该标识，既有普通/习惯计划语义不变。首次无项目设置边界暂时保留。
- UI 先按交接提交纯 MinimalIdlePanel/MinimalEndTimeSheet 及五视口/浅深/错误/忙碌样例，核心持有偏好、时间和命令；尚未有可用的极简模式入口。

## 数据与失败边界

### ACHIEVEMENT-27-01：轻量本地成就（用户已确认）

- 第一版为成就墙＋合并轻提示，不增加每日任务、奖励、账号或云同步。UI 成就入口在现有统计页内，不增加主导航页签；展示组件由 UI agent 实施，App 提示接线归核心。
- 初始稳定目录：完成专注 1/10/50 轮、实际专注 60/600 分钟、完成普通或习惯建筑 1/5 栋、7 个不同专注日（不要求连续）。普通完成与提前完成计轮数；中断只贡献实际时间，不计完成轮次或专注日。
- 使用已冻结完成日期，时区切换不重算历史日期；软删除源任务不删除已保留的专注/习惯建筑事实。建筑腐败不扣成就。确切解锁时间无法从保留事实推导时显示未知，不以当前时间伪造。
- 不新增解锁事实表：按当前有效备份事实投影，因此导入/回滚后随该备份重算。初次加载、导入和恢复是静默基线；不能补弹整个历史成就队列。用户操作带来的多个新解锁合并提示，不抢计时/汇报/离开警示焦点。
- 核心接口：`projectAchievements(state)`、`ApplicationService.achievementsProjection()`（每次 adopt 失效的只读缓存）、`newlyUnlockedAchievements(before, after)`；UI 使用 `unlocked` 判定状态，不能把 `unlockedAt === null` 等同未解锁。

验收：确定性、正常/提前/中断、跨日/时区、普通/习惯建筑、未知日期、删除/恢复、只读及初次静默均有测试；UI 与实际提示尚未接线时只标核心投影完成，不宣称功能可用。

### ORIGINOS-27-01：iQOO 原子岛适配（准入与设备待确认）

- 官方接入生态提供本地 Android 通知 extras 路径，不强制把本地专注数据转成云推送。vivo 原子通知接入权限与场景开关是前置条件；不拿挂号/外卖 demo 场景冒充专注。
- 标准 Android chronometer/Live Update 继续作为独立回退；厂商增强必须读取真实能力、保持单通知身份、幂等刷新/结束和已验收镂空小图标。不能为弥补系统倒计时模板每秒重发通知。
- 适配前确认具体型号、OriginOS/Android 版本、获准场景及系统自绘倒计时参数。通知/锁屏/息屏/原子岛分别验证，支持其中一种不能等同全部支持。
- 研究与外部前置条件见 `docs/research/ORIGINOS-LIVE-ACTIVITY.md`。当前未加入未经准入验证的原生 extras，未申请平台权限、引入 SDK 或进行云端传输。

验收：适配路径的本地参数/能力测试与 iQOO 真机矩阵；缺准入或设备时继续如实回退，不将可行性研究标为已适配。

- schema 11 增加活动/历史轮次 deferredSettlement 标识；schema 10 及以前按既有迁移链升级，不改旧轮次归属。备份封装及校验仍复用原实现，新格式不保证旧 App 可读；默认偏好键不变，极简开关将在本机偏好接线。
- 返回键处理纯展示层：不改变任何领域状态迁移；`exitAndroidApp` 仅在 world 空栈时调用。
- 材质 token 只影响渲染，不进入备份。

## 本轮核心验证检查点

2026-09-06 核心续推：主树全仓 typecheck、version:check exit 0；domain 129、application 53、storage 26、Web 88，共 296 单测通过。新增 minimal-focus 准备/提交控制器、前台分钟时钟、显式日选择、偏好迁移和通知恢复测试；App 延后结算继续/提前/取消/最终汇报已接线。提取 marathon-settlement 后，计划丢失恢复最终汇报，已删除宿主不遮住保留记录，无目标时结束仍写空分配结算。

新增 7 条 minimal-focus-continuity E2E 与 v22-marathon-lane、v24-marathon-settle、v5-focus-flow 同次 30/30、1.8 分钟、exit 0，产物 artifacts/minimal-core-continuity-final；7 条新用例从合成领域持久化记录验证真实 App 延续，不是未接入的极简 Sheet 首次启动验收。发布套件分类检查通过，新能力已纳入 core；未跑新的完整发布门禁。新 starter/分钟 hook 已备好，但 App 首次极简入口、开关/壳层、临时返回及 UI 展示仍等待 A4 组件验收；成就提示仍待接线。接口与两存储失败边界详见最新 UI 交接检查点。ADB 无设备，无 APK/安装/发布。

2026-09-06 新切片：全仓 typecheck exit 0；domain 129、application 51、storage 26、Web 58（共 264）单测通过，含新延后结算 9、轮次连续性 4、备份恢复 1 项。真实 Chromium 存储 E2E 1/1、4.6 秒、exit 0。旧资料迁移与当前 schema fixture 分开维护，未放宽普通任务的子任务校验。初轮发现习惯校验仍强制立即归入建筑，现已按显式新标识隔离，旧 58 项领域行为测试保留。

UI A3 树 1e2f7a4：类型/45 单测、10 定向 E2E（1.2 分钟）均通过；极简 Sheet 的键盘、忙碌、输入与样例主题仍按交接 A3-01～05 返修。核心下一步接 App 的 deferred 分支、最终汇报入口、极简本机偏好/启动、分钟时钟和原生通知文案；本轮没有将仅领域完成标为功能可用。

- 偏好/成就/共用结束时间：应用层 51、Web 54 单测，类型检查与 version:check 均 exit 0；当前版本保持 1.11.0 (26)。
- 主目录偏好定向 E2E 7/7，1.6 分钟；时间模块提取后 v21-marathon 7 passed / 1 既有 skipped，53.5 秒、exit 0，产物 artifacts/v27-marathon-time-extraction。两次为不同切片证据，不拼作完整发布门禁。
- 极简模式尚未接线；UI 成就待返修。iQOO 用户无真机，按最低 OriginOS 5 系统族研究，真实支持仍受能力/准入限制。没有 APK 或发布动作。

## 实施顺序

1. 同步本次确认范围与 UI 交接文档，保留已有初稿和历史验证记录。
2. UI agent 在独立 worktree 完成设置返修、共用组件、页面一致性和视觉资产清单，交回定向测试与截图。
3. 核心 agent 已开始偏好所有权拆分和成就投影；退出提示继续按真实生命周期证据定位，无真机不猜测改计数。App 其他职责、性能及 OEM 增强分批推进，不宣称本次小切片已完成整体重构。
4. 集成负责人审计 UI、最小接线与核心变更，同步现行规范和工作包，再安排增量验证 APK。
5. 用户确认准备发布后执行冻结树完整门禁与不可变候选流程。

## 历史进度（早期 A3/A4 快照，当前状态见顶部）

| 需求 | 状态 | 证据 |
| --- | --- | --- |
| UI-27-01 | A3 定向复验通过，视觉矩阵部分开放 | UI 树 1e2f7a4：Web 类型、45/45 单测、设置/开关/外观/坐标 E2E 10/10，1.2 分钟，exit 0；A1/A2 历史保留交接，未合并主树 |
| UI-27-02 | 受影响自动验证通过 | `back-layer.ts` 分层栈 + `useBackLayer`；App 根层与 WorldScreenV7 接线；单元测试 `4/4`，web `tsc` 与 Capacitor `27/27` 通过 |
| UI-27-03 | 受影响自动验证通过 | toast `{text, action?}` + "去设置"动作 + reduced-motion 门控；`v5-focus-flow` `13/13`、`focus-regressions` `3/3`、`v22-integrity-ended` 通过 |
| UI-27-04 | 受影响自动验证通过 | 沉浸带建筑标注（绝对定位保居中）、启动方块铺设动画、主题切换过渡；`v20-ambient-motion` `5/5`、`v21-marathon` `6/6`（1 例既有跳过）、`v22-integrity-ended`、`responsive-qa` 通过 |
| UI-27-05～08 | A3 部分通过，极简交互继续返修 | 成就 unlocked 契约、样式与暗色菜单 token 已改；極简键盘/输入/忙碌/主题证据仍开放，详见交接 A3-01～05 |
| MINI-27-01 | 核心连续性/汇报已接线；首次极简 UI 入口待集成 | 296 相关单测、全仓类型；本轮 7 新能力 E2E 加既有流程同次 30/30、1.8 分钟、exit 0。starter/分钟 hook 已备好；A4 展示返修、开关/壳层接线未完成，不宣称整项功能已可用 |
| RECOVERY-27-01 | 待实施 | 用户确认计数正确、既有提示仍未弹出；列为 V27 核心遗留项 |
| ARCH-27-01 / PERF-27-01 | 首个重构切片实施中 / 性能待测 | App 偏好解析/持久化/主题与玻璃应用移至 `focus-preferences.ts`、`use-focus-preferences.ts`；既有 props/key/mapping 保持，未改变 CSS。没有 A/B 性能提升结论 |
| ASSET-27-01 / TEST-27-01 / DOC-27-01 | 待实施；规范同步已开始 | UI 交接与现行材料规则已同步，资产流水线／核心测试优化仍未实施 |
| ACHIEVEMENT-27-01 | 核心投影已验证；UI/提示待接线 | 8 项稳定目录与 epoch 缓存，应用层 51/51 单测及类型通过；不写 schema/奖励；UI 组件按 A2 返修 |
| ORIGINOS-27-01 | 接入研究完成；适配前置条件未齐 | 找到本地通知路径；接入权限/计时场景、型号系统及真机待核实，不以研究代替已适配 |
| 真机验证 | 待设备与 UI 集成 | 2026-09-05 ADB devices -l 为空，本次未安装 APK |
