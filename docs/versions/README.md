# 版本工作包

- 每个产品版本使用一个 `VNN.md`，只记录该版本需求、数据影响、验收和最终状态。
- 当前工作只更新当前版本工作包；开始下一版本时新建文件。
- 版本完成后，工作包只接受事实纠正，不继续叠加后续版本内容。
- 当前产品事实属于仓库根目录 `Tomato Clock.md`。
- 工程和设计的现行规则分别属于 `ARCHITECTURE.md` 与 `DESIGN.md`。
- V1-V24 的旧流水仍保存在 `PROJECT.md`，但该文件已经冻结。
