# 2.0.0 后续改进工作包

## 当前执行顺序（2026-09-23）

最新用户决定（2026-09-23）覆盖下述旧“F04 暂停”与“先构建测试包”顺序：恢复 Minecraft Java 26.3 正式版材质/方块兼容；上版 2.0.0/37 由用户深度体验后再反馈，不立即重建。并行修极简空闲面板左右直接滑动、移除底部按钮/页码且高度回到前版，统计纪念建筑外层可折叠；尝试轻量面板雨滴/雷电。当前有效待办见 `docs/TODO.md`。

双构筑与好友服务器同步已完成代码及自动化验证，仍待用户真机验收。用户恢复 R01–R14 及其他非材质待办，继续暂停 F04 方块/材质兼容；本轮先完成这些反馈、全量门禁和可升级安装的测试包。现行范围与状态统一见 `docs/TODO.md`。下方早期非材质交付结果属于历史验证，不代表本轮真机反馈已解决。

建立：2026-09-12。状态：用户扩大下次测试包范围，实施 F04 之外全部后续项（2026-09-13）。当前 `version.json` 为 2.0.0/35，保留已有测试构筑与数据。

## 当前范围：非材质项完整测试包

2026-09-14 交付检查点：F04 之外的本轮实现与审计返工已合入工作树；最终同树 `test:unit:full` 为 711 常规＋25 扩展通过，`test:web:release` 为 162 通过、2 项既有明确跳过、0 失败/flaky。记录见 `artifacts/non-material-20260914/final-gate/`。用户要求停止额外截图补齐、直接构建；签名验证 APK 2.0.0/35 构建成功（JVM/lint/assemble exit 0），手机交付前重新连接，已覆盖安装 a50247d1 且 installed base.apk 哈希一致，无安装后 UI 巡检，故未上传测试仓库。APK 为 `artifacts/non-material-20260914/Blockcolc-v2.0.0-non-material-20260914-verification.apk`，SHA-256 `ed2e2a9d12e93aeb7004951f651e8060bbe680ae5bbfacbcce4714a3266c753c`。新纪念统计/轮次报告的额外五视口截图矩阵未完成，不将旧树截图当作当前验证；OEM 提醒与卡顿体感由用户实机反馈。仍不是正式发布候选。

覆盖下方首批 UI 优先出包顺序：用户确认不为单位调整单独出包，F04 材质兼容暂缓，其余 F01–F20（含设置返工）完成并集中验证后再构建测试 APK。中间允许必要的类型/单元定向验证；完整综合测试在实现与审计完成后统一运行。安装后不做代理手机巡检，由用户反馈。

2026-09-14 交付补充：构建完成时若手机未连接，仅将测试 APK 上传到已有私有仓库 `Dieight/blockcolc-test`（沿用仓库 APK 文件交付，不上传源码、报告或其他文件，不覆盖历史测试包）；回读下载并比较 SHA-256 后给用户链接。手机连接时仍走签名覆盖安装。此授权仅限测试仓库，不是正式应用发布。

已确认契约：F02 待继续的数字仍包含剩余专注与后续间隔休息；F18 升级首次进入统计把已满足的历史新成就集中展示一次、可全部关闭，以后只展示新解锁。不虚构历史获得时间，无法推导则明确未知。

### 批量所有权与依赖

1. 完成当前设置单位 worker 并审计，避免 Settings 文件与后续提醒开关并写。
2. 计时/原生包 F02/F14/F15/F16/F17：App、focus view/flow/round plan、通知/lifecycle 平台和设置偏好、首次创建。保持 domain 归属数据接口不变；负责给常驻 StatsScreen 传 active 页签标志，停止根层旧成就 toast，统计 worker 接管展示。
3. 世界包 F06/F07/F08/F13：voxel lighting/terrain/environment/renderer、蓝图预览适配及新增局部诊断。不得并写领域或通知；维持共享 renderer 与一次请求渲染路径。完整材质兼容 F04 不实施。
4. 数据/统计包 F19/F20/F18：domain 报告及验证、application 归属/建筑/成就投影、FocusReports、StatsScreen、BuildingMemoryPanel、各自 UI/CSS。新的普通分配按明确轮次数独占对应 session；习惯和普通共同守恒，未分配/旧 shared 歧义事实显式保留，禁止重复计时或平均伪造。首次进入统计由 active prop 驱动，回执只表示已展示，不改变成就真相。
5. F05 导入限额整链在 domain/world 写入者交回后实施，避免 validation.ts 与 renderer 并写；已证实 domain 仍有 48×48/100000 的更小上限，而 parser 横向限额为 96。需保证至少 60³/216000 非空气方块并保持已有更高高度兼容，验证输入/解析/保存/备份/恢复/预览和资源预算。
6. F11 用户于 2026-09-13 确认无刻度四时段色块预览方向，批准母版为 `apps/android/icon-source/blockcolc-four-periods-approved-20260913.png`；沿用现有启动图标派生脚本，检查小尺寸与裁切，通知镂空图标不动。

跨包文件不得并写：application-service.ts 与 ports.ts 由计时包持有；application index/model、domain 全部由数据包持有。现有 ReportMarathonFocus 应用映射使用 entry spread，可保留新增分配字段；数据包若需改服务实现，先提交独立补丁交集成，不直接并写。StatsScreen 新增可选 active:boolean，默认 false，App 传 tab==='stats'。数据包持有 command-runner 成就回调旧契约清理，计时包同时清理 App 调用；共同约定移除 publishAchievements 回调，普通 feedback/refresh/failure 不变。既有跨功能 E2E 不由多个 worker 并写，分别新增专属 spec，旧断言由集中集成处理。共享 Web E2E/dist 与 Android 输出目录仍串行，仅获得指定执行权的 worker 可运行。

## 当前切片：UI 优先测试构筑

用户确认先交付简单 UI 测试 APK，再推进复杂功能。首批 F01/F03/F09/F10/F12 已实现并交付测试 APK，其余待实施；使用 `Build-Verification-Apk.ps1`，保持 2.0.0/35，不发布、不清数据。2026-09-13 已通过忙碌检查，覆盖安装到授权 PJX110；待用户进行实际体验验收。

所有权：SettingsScreen/settings.css 与设置测试归 settings worker；TaskManagement 与 daily-goal CSS 归 goal worker；FocusStatsCharts 分配部分及 allocation CSS 归 stats worker；主代理持有 WorldScreen 活动文案、对应测试与产品/版本/设计文档，统一运行构建/E2E，避免共享 dist 被并行替换。

## 执行方式

Astra 持有需求、跨模块契约、集成与独立审计；Luna 执行有文件边界和可观察验收的小任务。主代理检查实际 diff/测试结果，不以子代理总结代替验收。先清点 live dirty tree，保留其他代理改动；每个共享文件同时只交给一位写入者。业务按下列所有权改造，移除被取代路径，不增加版本式 CSS、重复计时器或第二套通知状态机。

按用户要求一次交付完整有界工作包，等待该批结果全部返回后集中审计。2026-09-13 更新：全量测试和子代理仅在完成事件后检查，不再每 10 分钟轮询。测试 APK 推到手机后，代理不再做 UI 巡检，由用户人工反馈；构建及安装身份/哈希校验保留。

### F03 用户返工（2026-09-13）

用户指出单位占据右列不美观：将六处设置数字输入的单位移到左侧字段文字末尾（如“普通任务专注（分钟）”），去除数字框后的单位及占位列。所有数字框右边缘贴齐行内容右边缘；保留现有数字草稿、失败回退、输入范围和 44px 命中区。Luna 实施，Astra 审计；定向验证后构建新 verification APK，保留上一版 795fda40… 的日期归档，安装后不巡检手机。

配置准备已实测：父任务实时元数据为 `gpt-6-astra/medium`；未指定模型的只读子任务自动为 `gpt-5.6-luna/max`。未修改用户 `config.toml`。项目 skill 已移除重复旧版本事实，测试选择路由到 `docs/TESTING.md`，保留恢复/数据/签名哈希约束。

## 需求分类与验收

编号与用户原始清单一一对应。状态以当前切片及后续验收记录为准；原因猜测不是已确认诊断。

| ID | 范围 | 现有责任边界与验收 |
| --- | --- | --- |
| F01 | 极简/马拉松活动文字统一“专注中 第x/n轮” | 统一 focus view/face 文案，不能影响普通小任务名、已完成轮数或锁定计划宿主 |
| F02 | 全模式沉浸休息/待继续，分别“休息中”与“剩余专注总时间” | focus-view-state、focus-flow、round-plan、WorldScreen/FocusFace；检查固定轮次、习惯、马拉松、极简、自然结束/跳过/中断/重启/待汇报；移除非沉浸旧分支，休息无返回完整模式按钮 |
| F03 | 设置数字输入对齐并改善编辑体验 | 共享设置行/数字草稿组件及其 CSS；清空、中间编辑、范围/错误、失焦/Enter、持久化失败、浅深和字体缩放 |
| F04 | 材质包生效与完整 Java 方块视觉兼容 | transport→ZIP→block state/model→atlas/tint/geometry→cache/adoption 全链；按确切版本生成覆盖矩阵，对颜色、纹理、造型逐项对照；明确未知/缺失/回退原因，不能只加名称路由 |
| F05 | 确保 60×60×60 蓝图支持 | litematic/voxel validation、native transport、预览/正式世界；解析器现为横向96/高256，追查用户40×40瓶颈；测多区域包围盒、稠密216000格、空白体积、内存与备份恢复，不无意收紧既有支持 |
| F06 | 日月随时间运动，阴影方向同步 | 统一太阳/月亮位置、主光方向、世界旋转坐标与低频更新时间；固定晨/午/昏/夜时间验收，不重建地形、不仅改强度 |
| F07 | 通用与环境专属装饰 | 确定性放置/目录、环境约束、预算；花等通用物及海洋少量半露沉船，检查遮挡/相交/水位/密度；明确环境装饰与每日奖励归属，保护已有奖励位置与进度 |
| F08 | 新建建筑预览云堆积 | 小预览独立尺度、天空云分布/数量/裁切；分别对小大蓝图和主世界截图/帧耗时验收 |
| F09 | 今日目标内部弹层 UI | 复用设置行/输入和既有材料边界，主卡与弹层同时验收，目标开关/轮数/奖励不改变 |
| F10 | 替换专注分配标尺 | 统计只读投影不变，重新选清晰的比较式图形；保留应用绿色、真实分钟比例、长任务名、空数据及浅深主题 |
| F11 | 新图标方案，不要外围白色刻度 | 先概念预览与用户选择，再正式资产派生；不擅自改通知镂空图标 |
| F12 | 完整性说明含离开阈值且精简，开关同列 | 与 F03 共享设置行；阈值/次数来源真实，窄屏及大字仍可用，不复制写死默认秒数 |
| F13 | 各环境地基不低于应有地面 | terrain/building/road placement 共用支撑规则；自然山谷/经典空岛/海洋、小大蓝图与多建筑，保留海洋已修结果 |
| F14 | 休息/待继续双击至下一轮的卡顿 | 与 F02 一起测输入确认、命令队列、持久化、plan commit、renderer 更新；记录实际首反馈/首计时帧延迟，不跳过保存或虚假乐观完成 |
| F15 | 通知栏收起后旋转世界卡顿 | 原生注意力事实、生命周期回执、snapshot/存储、renderer generation与输入帧调度；同机同数据前后 trace，保持正确离开阈值、单次回执与保护结果 |
| F16 | 返回专注提醒，开关默认开启 | 休息截止且仍有轮次才发；极简/马拉松、后台/重启/权限拒绝/关闭开关/重复恢复/取消/最后轮幂等；同一状态映射流体云与通知栏，不每秒重发 |
| F17 | 首次创建普通/习惯二选一，必须且只创建一个 | setup/create orchestration 与既有创建命令；两类型、取消/返回、失败、重复提交、重启与已有用户不重走引导 |
| F18 | 成就统一 UI、统计入口检查/动画、扩充且展示锁定项 | application只读成就投影与独立展示回执；解锁条件/准确或未知时间、批量历史、导入回滚、重复进入、减少动态；不凭弹层改变业务事实 |
| F19 | 马拉松/极简汇报普通任务轮次归属，修正习惯统计 | 沿既有 FocusReports/ReportMarathonFocus/分配投影追查事实链；有变动的普通任务下提供类似习惯的轮次数汇报，保持可分配轮次守恒和一次结算；实际分钟、提前完成/中断、跨日与旧记录需明确，不假定一轮永远等于配置时长 |
| F20 | 统计页纪念建筑与小任务时间分布 | 复用保留的普通任务/建筑/小任务历史投影，提供独立纪念建筑板块，展示可追溯总量和小任务占比；按图表skill选型，浅深/长标题/空值/删除与恢复验证。旧数据缺少归属时明确显示未分配或不可追溯，不伪造比例 |

## 实施依赖与切片

1. **基线与合同**：确认当前代码/APK、冻结 F02 状态转移表，采集 F14/F15 真机卡顿基线。不是先继续上次完整回归。
2. **计时与原生闭环**：F01/F02/F14 同一主流程切片；F15 单独诊断边界；F16 依赖休息→待继续合同及原生幂等。不要让三位 worker 同时修改 App/flow/平台状态。
3. **共享表单与创建**：F03/F12/F09 共用控件/材料；F17 独立创建流程。可以与独立资源调查并行，集成时测整条用户路径。
4. **兼容底座**：先 F04 exact-version 数据清单与故障归因，再按模型族实现和对照；F05 同时检查 import budget，不当作改一个常量。资源扩展是最大的独立工作流，不能按剩余小修估算。
5. **世界与预览**：F06/F08/F13 明确同一 renderer 的更新/坐标责任后分片；F07 在地基、水位和资源能力稳定后接入。
6. **统计与品牌**：F10/F18 保持统计事实只读；F11 在预览批准后才入正式资源。各切片按 `docs/TESTING.md` 做增量验证；发布范围冻结后再完整门禁和精确候选验收。
7. **新增归属与纪念统计**：F19 先审计统一汇报的普通/习惯分配持久化与统计读取；冻结轮次到实际分钟的映射以及旧记录显示政策后实现。F20 依赖可追溯分配投影，在统计页独立呈现，不能只改图表掩盖缺失事实。两项待实施，不影响当前 UI 验证包出包顺序。

F19 只读代码核查：`projectFocusAllocation` 当前直接按历史 session.projectId（宿主）汇总；领域 `ReportMarathonFocus` 已把前 K 个 session ID 交给习惯建筑，但剩余 session ID 被每个普通小任务报告共同引用（shared=true）。因此不能直接把每条普通进度报告的时长相加，否则重复计时。后续需独占分配与兼容旧 shared 事实；当前没有修改结算或统计读取算法。

## 待冻结事项与非承诺

只读勘察（不是故障复现）：Luna 已定位 `packages/resource-pack/src/index.ts` 的 accepted/rejected/issue 清单，`block-models.ts` 的状态与模型解析，`atlas.ts` 的逐块映射，`packages/voxel/src/resource-textures.ts`/`resource-geometry.ts` 的纹理/几何批次与回退，以及 `renderer.ts` 施工 reveal 暂用原创材质的分支。主代理抽查了 manifest issue 返回、动画限制、几何批次预算及 reveal 路径。后续需用用户实际未生效的包确认命中哪条路径；目前不能把“解析/路由”猜测写成已定位原因。

- F02“剩余专注总时间”的数字是否继续包含后续间隔休息，需要在状态表中写清。之前规则为剩余专注+间隔休息；本次文字不能被当作未经确认的时长算法变更。
- F04 默认沿用当前 Java/Litematic 范围。2026-09-12 已找到官方 [26.3 Release Candidate 2](https://www.minecraft.net/en-us/article/minecraft-26-3-release-candidate-2) 资料，故目前没有理由直接回退26.2；实施时重新固定确切版本、DataVersion、pack format与注册表。预览资料存在不等于已取得全部方块/模型数据，也不等于已支持。
- “所有方块”包括特殊渲染模型的覆盖验收，但不自动包含红石模拟、实体AI、任意Shader/mod/CTM/PBR扩展。原版完整外观对照依赖合法本地资源；不随APK分发未经授权资产。资源包能导入不等于每个方块被实际应用。
- F05 以“至少保证60立方可用、不缩小已支持内容”为暂定解释，入口限制调查后再冻结阈值与预算。
- F07 环境常驻装饰与每日目标奖励需分清；花、沉船的风格/密度先预览。F18 新目录、首次升级是否补弹、批量队列及已展示回执是否随备份迁移仍待设计，不虚构未知解锁时间。
- 不给未测设备帧耗时或 OEM 流体云表现作保证。2026-09-12 的“不新增安装/版本授权”已被后续测试包授权取代；仍未获得正式发布或清数据授权。

## 准备验收记录

### 首批 UI 验证（2026-09-13）

F01/F03/F09/F10/F12 已实现并完成受影响浏览器验收，测试 APK 已构建安装，待用户体验反馈。Web typecheck 和 188/188 单测退出 0；最终 production mobile-chromium 综合回归 58 通过、1 个既有跳过、退出 0（`artifacts/ui-verification-20260913/final-e2e`）。跳过项为旧沉浸重置视图自动隐藏场景，不能计为通过。五视口浅深截图及大字、失败写入、重载与焦点交互已检查；图表为 G3 模板横向离线适配，不改变 F19 归属算法。

此前设置保存回退与真实大字越界已修复；旧今日目标文本断言已按实际卡片/弹层更新并保留持久化检查。失败/中断记录保留，不将本次增量验证等同于完整 149 项发布门禁。完整审计记录位于 `artifacts/ui-verification-20260913/audit.md`。

`Build-Verification-Apk.ps1 -Serial a50247d1` 退出 0：真实 fixture 哈希、Web build、Android JVM/lint/release assembly、包名/版本/正式签名均通过。已安装 `com.blockcolc.app` 2.0.0/35；构建、verification 副本、手机 base.apk 与日期归档 APK 的 SHA-256 均为 `795fda40c2b5baab800022209e0df74cf19338536635d333c5a36c364547fb5e`。交付文件 `artifacts/ui-verification-20260913/Blockcolc-v2.0.0-ui-20260913.apk`，同目录保留 verification-evidence.json。启动、既有世界、设置输入对齐、目标弹层与原生返回关闭、统计分配条已在手机检查；未修改目标、设置或业务进度，未新建测试任务。完整发布门禁未运行，用户验收未代签。

### 初始准备（2026-09-12，历史记录）

- 配置TOML解析通过；无模型覆盖的 Luna 子任务运行并返回真实代码证据。父/子模型身份由运行元数据核对，不依赖自我介绍。
- Skill 原件已备份于 `artifacts/skill-audit-20260912/original`，逐文件SHA-256比较一致；结构验证通过。删除的是冗余说明和旧约束，不是测试、历史APK或业务代码。
- 独立 Luna 场景演练已完成：窄 UI 改动保持业务边界；资源包只诊断不修；统一休息先核对 dirty 所有权及恢复/汇报；准备请求不继承旧安装动作。主代理复核输出，未发现需要恢复旧强制全量门禁或旧产品限制的理由。该演练只验证工作选择，不是应用行为测试。全套skill由15884缩至10854字节（约32%），引用链接均存在。
- 18个需求编号唯一且无遗漏；`version:check` 确认2.0.0/35。未改用户配置、业务源代码或正式图标。
- 本轮仅修改skill及范围文档；18项均未声称实现，未运行应用构建或安装。旧完整回归/设备安装结果不代表本工作包通过。
